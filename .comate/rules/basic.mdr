---
description: 编码基本规范 - 适用于所有代码文件
globs:
alwaysApply: true
---

## 代码风格指南

### 1. 第三方库规范

- **UI 组件库**
    - 所有 UI 组件统一使用 `antd` 6.x
    - 使用 antd class 覆盖样式时，前缀为 `.ant-5`
- **样式方案**
    - 优先使用 `@emotion/react`、`@emotion/css`、`@emotion/styled`
    - 禁止使用内联样式，除非有特殊需求
- **表单处理**
    - 统一使用 antd 的 Form 组件

- **文件大小限制**
    - 单个文件行数不超过 **150 行**（建议控制在 140 行以内）
    - 超过限制时应拆分为多个文件或提取公共逻辑

### 2. 公共模块组织

- **工具函数**
    - 统一放在 `src/utils/` 文件夹下
    - 通过 `@/utils` 别名引用
    - 工具函数应保持纯函数特性，无副作用

- **API 定义**
    - 按业务方向组织：`src/api/<业务模块>/`
    - 使用 `axios` 进行 HTTP 请求
    - 每个 API 文件应包含请求和响应的 TypeScript 类型定义

- **日期处理**
    - 统一使用 `dayjs` 处理日期
    - 格式化使用 `YYYY-MM-DD HH:mm:ss` 格式（dayjs 格式）

### 3. 页面和组件规范

- **页面布局**
    - 新建页面统一使用 `@/design/Layouts/PageLayout` 组件
    - 页面级组件放在 `src/<业务模块>/` 目录下
- **组件目录结构**
    - 组件放在 `src/components/` 对应业务目录下
    - 每个组件应有独立目录，包含组件文件、样式文件和类型定义

## 编码原则

### 核心原则

1. **功能一致性优先**
    - 重构时必须保证功能行为不变

2. **依赖管理**
    - 优先使用项目已安装的第三方库
    - 添加新依赖前请先检查 `package.json`
    - 不确定库的使用方式时，使用 Context7 MCP 工具查询官方文档

3. **代码组织**
    - 组件内的纯函数（不依赖 state、props、hooks）应放在组件外部
    - 纯函数与组件定义在同一文件中，位于组件定义之前
    - 导入语句按以下顺序组织：
        1. React 相关
        2. 第三方库
        3. 项目内部模块（使用 @ 别名）
        4. 相对路径导入
        5. 类型导入（使用 `import type`）

4. **React 导入规范**
    - 避免 `import React from 'react'`
    - 按需导入实际使用的 API：`import { useState, useEffect } from 'react'`

5. **样式变量**
    - 颜色变量优先从 `src/constants/colors/` 引入
    - 只有找不到对应色值时才直接使用十六进制色值

## 项目启动指南

### 环境准备

1. **Node.js 版本**
    - 检查本地 Node.js 版本：`node -v`
    - 要求版本：Node.js >= 18
    - 未安装或版本不符时，安装 LTS 版本

2. **包管理器**
    - 检查 Yarn 是否安装：`yarn -v`
    - 未安装时，全局安装：`npm install -g yarn`

3. **依赖安装**
    - 进入项目根目录
    - 运行：`yarn install`
    - 等待依赖安装完成

4. **启动开发服务器**
    - 运行开发命令：`yarn start`
    - 浏览器访问：`http://localhost:3000`（或命令行提示的端口）

### 常见问题

- 依赖安装失败：尝试删除 `node_modules` 和 `yarn.lock`，重新运行 `yarn install`
- 端口占用：修改 `vite.config.ts` 中的端口配置或关闭占用端口的进程
