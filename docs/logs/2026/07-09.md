# 2026-07-09

## Global Content Layout Padding

- Added `docs/plans/2026-07-09-global-content-layout-padding-plan.md` for the Figma-informed AppLayout content padding baseline.
- Updated `WorkspaceContentLayout` so the routed content area owns default page padding and preserves the existing theme-driven content background.
- Updated `docs/design/app-overview.md` to record that AppLayout owns routed content inset, scroll canvas, and content area background.
- Verification: `yarn lint-type` passed.

## Page Wrapper Convergence

- Scanned routed pages for duplicate outer padding, background, viewport height, and page-level wrappers after the global content inset change.
- Removed duplicate outer padding/background wrappers from `BorderGlowDemo`, `RuntimeConfig`, `StartupConfig`, and the route loading state.
- Moved the `AIChat` full-bleed exception into `WorkspaceContentLayout` so immersive pages are declared by the global shell instead of using page-local negative margins.
- Updated `PageLayoutHeader` spacing to rely on tokenized bottom spacing without adding page-level top margin.
- Removed stale `noMarginTop` usage and deleted the unused legacy `PageLayout` page wrapper implementation so the layout entry no longer owns page-level padding/background/min-height behavior.
- Verification: `yarn lint-type`, `yarn test`, and `yarn build` passed. Targeted eslint passed for changed source files except `Applications/index.tsx`, which still fails on its pre-existing `max-lines` violation; full `yarn lint` still fails on pre-existing unrelated files.

## Workspace Flex Layout Correction

- Reworked the workspace body so primary navigation, secondary navigation, and routed content participate in one horizontal flex layout instead of using fixed positioning plus manual left offsets.
- Updated the routed content frame to occupy the remaining workspace width, with the visual-spec spacing of 24px vertical and 32px horizontal owned only by the content padding layer.
- Verification: `yarn lint-type` passed.

## Workspace Content Layout Type Fix

- Converted private styled props in `WorkspaceContentLayout` to `$`-prefixed props and explicitly typed Emotion interpolation parameters to avoid implicit `any` diagnostics.
- Verification: `yarn lint-type` passed.

## Layout Detail Audit Follow-up

- Removed the remaining routed-content fixed/offset assumptions from the detail audit scope and kept `WorkspaceContentLayout` as a flex child that fills the remaining workspace area.
- Kept the content spacing as a single `WorkspaceContentLayout` padding layer: standard pages use 24px vertical and 32px horizontal token spacing, while `/ai-chat` is full-bleed by padding exception only.
- Removed the `/ai-chat` route-specific dark content background so the page remains on the global theme surface while retaining full-bleed layout behavior.
- Tokenized AppLayout top navigation, primary navigation, secondary navigation, fallback header, and AIChat local styles; removed scoped hardcoded hex/rgba and inline style usage.
- Filtered styled private props for AntD `Content` and `motion.div` wrappers to avoid leaking `$` props.
- Verification: `yarn lint-type`, targeted `yarn eslint`, `yarn verify:design-tokens`, `yarn test`, and `yarn build` passed. Independent scoped detail audit passed. Full `yarn lint` and `yarn format:check` still have pre-existing unrelated failures.

## Secondary Navigation Style Type Fix

- Moved collapsed secondary sidebar dynamic styles into an explicitly typed helper to avoid IDE implicit `any` diagnostics for `$collapsed` in Emotion template interpolations.
- Tightened secondary navigation button styles by preserving selected hover state and inheriting button font where browser defaults could leak through.
- Verification: `yarn lint-type`, targeted `yarn eslint`, targeted `yarn dprint check`, and `yarn verify:design-tokens` passed. Full `yarn lint` still has pre-existing unrelated failures.
