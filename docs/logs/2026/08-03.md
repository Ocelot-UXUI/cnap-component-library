# 2026-08-03

## Workloads 页面细节优化 req1/3/4 实现（Plan A）

依据 `docs/plans/2026-08-03-workloads-detail-optimizations-plan.md`（独立 draft review 通过 → planned）实现。三份计划均做独立 review：baseline 的 file:line 经子代理逐条核实属实；Plan A 无依赖先行；Plan B（全局弹窗/抽屉机制）review 通过转 planned；Plan C 仍 proposed（gated 于 Plan B 关闭）。

- req1 Pod 版本号截断：`podCells.tsx` 新增纯函数 `truncateVersion`（>12 时前 5 + `...` + 后 4）与 `renderVersion`（仅截断时用 Tooltip 显示完整 `v{version}`）；补单测 `__tests__/truncateVersion.test.ts`（边界 12/13/超长）。
- req3 弹窗 Group/容器默认选择：`operations/shared/loader.ts` 的 `aggregateContainerNames` 返回 `string[]`→`ContainerOption[]{ name, type }`，`WorkloadsBundle.containerNames` 同步；三个 machine（restart/verticalScale/horizontalScale）`loadingGroups.onDone` 新增「无 defaultGroupId 时取 `event.output[0]?.id` 自动选第一个 Group」分支；`loadingWorkloads.onDone` 容器选择由 `[0]` 改为优先 `type === 'MAIN'`、fallback 首个；三个 Modal 容器 Select options 适配 `{ name, type }`。
- req4 Group 操作列表与弹窗传参：`GroupHeader.buildMenu` 过滤 `Workload||None`→仅 `Workload`，新增 `onWorkloadOperation` 回调、`handleMenuClick` 命中 operation.name 时触发；`WorkloadsHeader.isHeaderOperation` 过滤 `None||Workload`→仅 `None`；`PodGroupTable` 透传 `onWorkloadOperation(group.id, op)`；`PodContentArea`（方案 A）自持一份 `WorkloadOperationModals`（activeOp/activeOperationName/activeGroupId），命中 `MODAL_CAPABILITIES` 时打开并带入 `defaultGroupId = group.id`。
- 附带修复三处 `operations/*/__tests__/rows.test.ts` 预存 lint-type 错误（`RuntimeWorkloadContainer` fixture 缺 `type`/`image`）——req3 使 `type` 成为 load-bearing 字段，顺带补齐。

验证（占用解除后 `yarn install` 成功并运行）：

- `yarn lint-type` ✅ 通过（0 error）——覆盖 req3 返回类型变更（`ContainerOption[]`）与三处测试 fixture 补 `type`/`image`。
- `yarn test` ✅ 变更涉及测试全绿：`operations/{restart,verticalScale,horizontalScale}/__tests__/rows.test.ts` 与新增 `__tests__/truncateVersion.test.ts`。其余 6 个失败 + 3 个失败套件 + 4 个 unhandled error 均在未改动文件、与本次无关（navigation context accountId 数字/字符串断言、sidebar url、GPU 合并、jsdom 缺 `ResizeObserver`、axios `Network Error`），为预存/环境问题。
- `yarn lint` ✅ 无新增 error（4 个预存 error 全在未改动文件，与基线一致）；`PodContentArea/index.tsx`（146→177 行）与三个 operation machine 触发 max-lines 警告，与仓库既有 max-lines 警告基线（MountVolumeList/ResourceLimit 等）同类，非阻塞。
- 期间曾遇环境阻塞：`yarn install` Link 步骤 EPERM 无法 unlink 被占用的 `@rolldown/binding-win32-x64-msvc` 原生二进制；用户释放占用后重装成功。

Plan A 已通过 independent closure audit（独立子代理复核 + 复跑三命令）关闭为 `completed`；plan `## Closure` 已落评审证据。Plan B/C 按用户要求暂挂起。代码变更尚未提交（working tree），待用户决定提交时机。

- Files: `PodContentArea/{podCells.tsx, GroupHeader.tsx, PodGroupTable.tsx, index.tsx, __tests__/truncateVersion.test.ts}`, `WorkloadsHeader/index.tsx`, `operations/shared/loader.ts`, `operations/{restart,verticalScale,horizontalScale}/{machine.ts, *Modal/index.tsx, __tests__/rows.test.ts}`

## 容器日志与 Web 终端接口改为 WebSocket

远端 ku 文档更新：查看容器日志和 Web 终端改为 WebSocket 通信。本次仅同步接口文档与 API 层实现，不涉及业务组件 UI 改动。

### 源文档同步

- `docs/input/source-api-runtime-workloads.md`：接口总览表新增两条 WS 路径（`/ws/v1/.../logs`、`/ws/v1/.../terminal`）；查询容器日志章节新增 "WebSocket 模式" 子节；新增 "Web 终端" 章节（含 `v5.channel.k8s.io` 协议说明与 channel ID 表）。

### API 层变更

- `src/api/runtimeLogStream.ts`：从 HTTP fetch + ReadableStream 改为 WebSocket。`buildContainerLogUrl` → `buildContainerLogWsPath` / `buildContainerLogWsUrl`（`/api/cnap/ws/v1/` 前缀）。认证头（`x-region`/`x-account-id`/`baggage`）因 WebSocket 无法设置自定义 header，转为 query 参数传递。`streamContainerLogs` 接口签名不变（params/handlers/controller 保持一致），`useContainerLogStream` hook 无需改动。
- `src/api/runtimeTerminal.ts`（新增）：基于 `v5.channel.k8s.io` 协议的 WebSocket 终端连接。导出 `connectContainerTerminal`（返回 `sendInput`/`resize`/`close` 控制器）、`buildContainerTerminalWsPath`/`buildContainerTerminalWsUrl`。binary frame 首字节为 channel ID（0=stdin/1=stdout/2=stderr/3=error/4=resize），resize payload 为 `{Height, Width}` JSON。
- `src/api/runtimeLogStream.test.ts`：测试改为验证 `buildContainerLogWsPath`（`/api/cnap/ws/v1/` 前缀）；mock `getCommonOptionsForAppspace` 避免 localStorage 依赖。`createLineAssembler` 测试不变。
- `vite.config.ts`：proxy `/api` 增加 `ws: true`，支持开发环境 WebSocket 代理。

### 未纳入范围

- `ContainerTerminal.tsx` UI 组件实现（xterm.js 终端渲染）—— 接口层已就绪，业务组件待后续实现。
- `ContainerSubTabs.tsx` Props 透传 —— 待终端 UI 实现时一并处理。
- `runtimeResource.ts` 的 `getContainerLogs` HTTP GET 接口保留（远端文档仍列出该端点，用于非流式场景）。

验证：

- `yarn lint-type` ⚠️ 3 个预存错误（`RuntimeWorkloadContainer` 类型在 `horizontalScale`/`restart`/`verticalScale` 测试中缺少 `type`/`image`，与本次改动无关）
- `yarn test src/api/runtimeLogStream.test.ts` ✅ 6 passed
- `yarn lint` ⚠️ 4 error + 4 warning 全在未改动文件（与 07-31 基线一致）

## 组件视觉规范 Playground 落地

- 新增独立 `playground` 路由页面（左右布局），集中展示 18 项 antd 组件改造后的视觉效果，供测试/本地批量核验。
- 需求：`docs/requirements/cnap2-component-playground.md`
- Plan：`docs/plans/2026-08-03-component-playground-plan.md`（draft review ACCEPT → planned → 实现完成）
- 新增文件：
  - `src/pages/ComponentPlayground/index.tsx` — 页面外壳（左侧列表 + 右侧内容切换，同一时刻仅渲染选中组件）
  - `src/pages/ComponentPlayground/ComponentPlayground.style.ts` — 左右布局样式（`@emotion/styled`，引用 design tokens，无 hex）
  - `src/pages/ComponentPlayground/sections/SectionShell.tsx` — section 共享外壳
  - `src/pages/ComponentPlayground/sections/Buttons.tsx` — Button / Checkbox / Switch / Radio
  - `src/pages/ComponentPlayground/sections/Inputs.tsx` — Text Input / Number Input / Select
  - `src/pages/ComponentPlayground/sections/Overlays.tsx` — Drawer / Modal / Message / Table
  - `src/pages/ComponentPlayground/sections/Display.tsx` — Tag / Pagination / Empty / Tooltip / Breadcrumb
  - `src/pages/ComponentPlayground/sections/index.tsx` — 18 项 section 注册表（含 SearchBox / Collapse Header 占位）
- 修改文件：`src/routers/index.tsx` — 注册 `playground` 路由 + 生产守卫（`import.meta.env.PROD` 时重定向至 `/home`）
- 不改动 antd 组件改造代码（presets.ts / reset.css / global-table.ts 保持不变）
- 验证：lint-type 通过；lint 新增文件无告警（基线预存 4 errors / 4 warnings 与本次改动无关）；build 通过

## 日志/终端需求文档补齐（WebSocket 对齐）

回顾 Workloads 日志/终端需求文档，发现两处缺口并补齐（纯文档，无代码改动）：

- `docs/requirements/pod-container-logs.md`：解除 SSE↔WebSocket 真源冲突。原文档头号阻塞项为"HTTP 流式有问题、拟改为 SSE"，与远端 ku 文档 2026-08-03 更新（改为 WebSocket）冲突。已更新状态（未完成→进行中）、传输层说明、API 依赖（`getContainerLogs` HTTP → `streamContainerLogs` WebSocket `/ws/v1/`）、Open Questions（移除 SSE 阻塞项，暂停/续接重做降级为非阻塞实现级待办）。
- `docs/requirements/pod-container-terminal.md`（新增）：此前终端无独立需求文档，仅 `pod-detail-drawer.md` 4d 节的 UI 骨架 + Open Question。依据 Figma 终端 Tab 截图（连接/断开切换、清屏禁用/hover 提示、操作按钮 tooltip）+ WebSocket 接口（`v5.channel.k8s.io`）+ `runtimeTerminal.ts` 补齐完整需求：Goal/In-Out Scope/工具栏/User Flows/Business Rules/Edge Cases/Acceptance/API 依赖/Open Questions。引用 `WEBSSH_TERMINAL_GUIDE.md` 为 xterm.js 实现基线。
- `docs/requirements/pod-detail-drawer.md`：Open Questions #1（终端技术方案）标记为已定并指向新终端需求文档；#2 指向日志需求文档。

## 日志/终端实现 plan 落盘

依据补齐后的需求，创建两份执行 plan（均 Status: proposed，Owner: v_wangkaiyuan02，实现前待 draft review）：

- `docs/plans/2026-08-03-pod-container-logs-websocket-plan.md`：容器日志 WebSocket 已就绪，剩余「暂停/续接重做」（暂停不断流 + 缓存 3min + 开启 append）+ 切换/断线校验 + 验证收口。3 阶段。
- `docs/plans/2026-08-03-pod-container-terminal-plan.md`：终端从占位升级为真实交互——引入 xterm.js（`@xterm/xterm`+`@xterm/addon-fit`）、接入 `connectContainerTerminal`（`v5.channel.k8s.io`）、连接/断开/清屏/resize/全屏 + props 透传 + 验证收口。4 阶段。
- 按"一个计划一个结果面"拆分为两 plan（日志与终端关闭条件独立）。

## 终端 Open Questions 收敛（owner 决策）

Owner 明确三项，已回填 `pod-container-terminal.md` 与终端 plan：

- Shell 选项定为 `/bin/bash` / `/bin/sh`（默认 `/bin/bash`），选中值原样作 `command` 参数（原截图"current"作废）。
- 断开后保留终端最后画面（不清空）。
- 终端渲染库最终定为 **xterm v6**（`@xterm/xterm` + `@xterm/addon-fit`）：先按指南拟用 v4，owner 复核后改用 v6（v4 已停维护、与 React 19/Vite 8 兼容性存疑；v6 API 与 v4 基本一致，仅包名/CSS import 路径差异）。实现模式仍参考 `WEBSSH_TERMINAL_GUIDE.md`（v4，按 v6 适配）；不用 attach addon（走自定义 `v5.channel.k8s.io` 帧）。需求与终端 plan 均已回填 v6。

## 日志/终端 WebSocket 实现（两 plan draft review → 执行）

两 plan 各做独立 draft review（General subagent），均 FAIL 后修正收敛为 planned，再实现。验证：`yarn lint-type` ✅ / `yarn test`（logLine 14 + runtimeLogStream 6）✅ 20 passed / `yarn lint` 改动文件无告警（其余为既有/Plan A 基线）/ `yarn build` ✅（xterm v6 打包进 Workloads chunk）。两 plan 转 partially completed（运行时手测 + independent closure audit pending）。

### 日志（plan 2026-08-03-pod-container-logs-websocket）

- draft review 关键结论：默认 `following=false` 与需求「默认跟随」AC 冲突，且新设计下默认暂停会把首包历史打进缓存被丢弃 → 纳入 Phase 1。
- `ContainerLogs.tsx`：默认 `following=true`。
- `useContainerLogStream.ts` 重写：连接生命周期与 following 解耦（`active=fileReady`，不再随暂停 abort）；`followingRef` 读实时暂停态；following 时 `onLine` 直接 append，暂停时写 `cacheRef`（按 `Date.now()` 到达时间 + `pruneCache` 3min 窗裁剪）；暂停→开启 flush 缓存并 `trimLogLines` 封顶；切换容器/来源/reconnect 仍清空重载。
- `logLine.ts`：新增 `CachedLogLine`/`PAUSE_CACHE_WINDOW_MS`/`pruneCache`；`logLine.test.ts` +3 用例。

### 终端（plan 2026-08-03-pod-container-terminal）

- draft review 关键结论（3 blocking，均已修）：(1) API 无 open 信号 → `ContainerTerminalHandlers` 加 `onOpen?` + `socket.onopen`，驱动 connected 与首次 resize（否则 CONNECTING 态 resize/输入被丢）；(2) `rgba(12,12,12,1)` 违反 design-token → 改 `semantic.logConsole.bg/.text`；(3) 单文件超 150 行 → 拆 `ContainerTerminal/` 目录。
- `runtimeTerminal.ts`：加 `onOpen?`；`TextDecoder` 改 stdout/stderr 分通道共享 `{stream:true}`（消除跨帧多字节 UTF-8 乱码）。
- 新增 `ContainerTerminal/{index.tsx, useContainerTerminal.ts, ContainerTerminal.style.ts}`（各 ≤150 行），删除原占位 `ContainerTerminal.tsx`；`ContainerSubTabs` 透传 props。
- xterm v6 挂载 + `@xterm/addon-fit` + ResizeObserver 自适应；连接/断开切换（断开红态 `semantic.state.error.default`）、stdin↔onData / stdout↔write、`onOpen` 首次 fit+resize、清屏（未连接 `<span>` 包裹 Tooltip 提示 + 禁用）、全屏切换后 `syncSize`、在窗口打开入口占位；切换容器/卸载 `close()`。
- 依赖：`@xterm/xterm@6.0.0` + `@xterm/addon-fit@0.11.0`（锁定）。

## 终端联调修复：去掉 WebSocket 子协议

联调发现终端握手被后端拒（1006）：`new WebSocket(url, ['v5.channel.k8s.io'])` 携带子协议导致失败。对照 `WEBSSH_TERMINAL_GUIDE.md:248`（`new WebSocket(url)` 不带子协议、channel 帧应用层手拼）确认后端为自定义 relay、不做子协议协商——`v5.channel.k8s.io` 只指帧格式名，不是 WS 子协议。

- `src/api/runtimeTerminal.ts`：去掉第二参数 → `new WebSocket(url)`，binaryType + channel 帧不变。
- `docs/bugs/04-terminal-websocket-subprotocol-handshake-rejected.md`（新增）：记录该非显而易见坑。
- `docs/requirements/pod-container-terminal.md`：协议 Business Rule 补"握手不携带子协议"。
- 验证：`yarn lint-type` ✅ / `yarn build` ✅；去掉后握手成功（联调实测）。

## 补充后端说明（终端接口）

后端补充：终端接口已加上，按 cnap1.0 对接 EKS 的协议实现（前端复用即可连通）；**当前无鉴权步骤**；后续认证/授权就绪后再迁移 EKS/SCI 双通道与认证链路。已回填：

- `docs/input/source-api-runtime-workloads.md`「Web 终端」节：新增"后端补充说明（2026-08-03）"引用块。
- `docs/requirements/pod-container-terminal.md`：鉴权 Business Rule 改为"当前端点无鉴权、query 认证为前瞻兼容"；Out Of Scope 增"EKS/SCI 双通道 + 认证链路由后端后续推进"。
- 印证前述判断：统一端点当前不含 EKS/SCI 分支，是后端后续迁移项，非前端本期范围。

## 全局弹窗/抽屉注册与调用机制（Plan B 实现）

依据 `docs/plans/2026-08-03-global-modal-drawer-registry-plan.md`（planned，draft review 通过）实现。用户从"暂挂起"转为要求实现；owner 校验通过（`git config user.name` = v_wangkaiyuan02，在 Owner 列表）。

- 新增 `src/overlay/` 模块（app 基础设施，层级同 routers，可引入 pages）：
  - `types.ts`：`OverlayManagedProps`（宿主注入 open/onClose）、`ModalRegistry`（key→组件 props，单一真源）、`ModalKey`/`ModalInvocationProps<K>`/`ActiveModal`；抽屉轴对称类型（`DrawerKey = never`，暂无注册项）。仅 `import type` 页面组件 props，无运行时页面依赖。
  - `overlayState.ts`：纯 `overlayReducer`（弹窗单槽 + 抽屉单槽两独立轴；open* 覆盖式替换实现同轴互斥；close* 无活动项返回原引用）。无 React。
  - `OverlayContext.tsx`：`constate` Provider + `useOverlay`；`useReducer` 持状态，`useLocation` 驱动路由切换 `closeAll`（全局单实例清理，解决 Phase 1 剩余风险）。
  - `registry.ts`：`MODAL_COMPONENTS`（`registerModal<K>()` 登记时按 key 校验组件 props）+ `DRAWER_COMPONENTS`（空）。唯一引入页面组件的运行时文件。
  - `OverlayHost.tsx`：按 key 渲染活动 overlay，注入 open/onClose，透传 props。类型擦除仅限 registry 登记点与 Host 渲染边界；对外 `openModal<K>` 完全类型安全。
  - `index.ts`：导出 `OverlayProvider`/`useOverlay`/`OverlayHost` 与 key/props 类型。
- `AppLayout/index.tsx`：`<OverlayProvider>` 包裹 `AppLayoutBody` + `<OverlayHost />`，置于 `ConfigProvider` 内（继承 antd 主题）、RouterProvider 内（useLocation 可用），覆盖全部子路由。
- Pod 操作弹窗接入（首个消费者）：三个 Modal（`BatchRestartPodModal`/`BatchPodDeleteRebuildModal`/`BatchPodForceDeleteModal`）props interface 加 `export`，登记为 `pod-restart`/`pod-delete`/`pod-force-delete`。`Workloads/index.tsx` 移除本地 `modal` 状态、`ModalKey` 类型与三处条件渲染，改经 `useOverlay().openModal`（行内 `CAPABILITY_TO_MODAL` + 批量 `BATCH_KEY_TO_MODAL` 映射到全局 key）；`onSuccess` = 清空选择 + `closeModal`。
- 抽屉轴：机制/API/reducer 已就绪并与弹窗轴对称，现有抽屉（Pod 详情/YAML）迁移列为 follow-up（Non-Goal），故注册表暂空、`DrawerKey = never`；互斥/并存/清理由 reducer 单测证明（不依赖真实抽屉组件）。
- 文档：新增 `docs/architecture/overlay-registry.md`；`module-boundaries.md` 补 `overlay/` 模块与依赖规则；architecture `README.md` 补 owner doc。

验证：

- `yarn lint-type` ✅ 0 error。
- 类型安全反例证明：临时 scratch 文件用 `@ts-expect-error` 断言「漏字段」「未注册 key」均报错，`tsc` 通过（expect-error 被消费）即证明约束生效；验证后删除该文件。
- `yarn test src/overlay` ✅ 7 passed（overlayReducer：弹窗互斥、抽屉互斥、二者并存、closeModal/closeDrawer 只清各自轴、closeAll、空态返回原引用）。
- 变更文件 `yarn eslint` ✅ 无 error/warning（overlay 各文件 < 140 行）。
- `yarn format`（dprint）仅格式化本次变更文件（未触碰仓库既有未格式化文件）。
- 全量 `yarn test`：6 failed | 182 passed | 16 skipped + 3 unhandled（ResizeObserver）——全部落在未改动文件（clusterContext/NavigationContext/navigationContextMachine/derive/navigationTool/usageMerge），与 07-31 及 Plan A 基线一致，非本次引入。
- 打包影响：`OverlayHost` 经 registry 引入三个 Pod 弹窗，随 AppLayout 进入非 lazy chunk（此前随 Workloads 页 lazy 加载）；组件体量小，可接受。

Plan B 各 Phase 转 completed、Exit Criteria 勾选；独立 closure audit（General subagent）判定 PASS 后 Plan Status 转 `completed`、Closure Gates 全勾选。audit advisory「补永久负向类型测试」已落地：新增 `src/overlay/__tests__/overlayTypeSafety.test-d.ts`（`@ts-expect-error` 锁定漏字段/混入受控 props/错 key 报错，tsc 校验，不参与 vitest 运行）。代码变更未提交（working tree）。
