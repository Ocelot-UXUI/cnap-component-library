# 2026-08-10

## 落盘 CNAP 2.0 流量接入模块原始需求文档到 docs/input

- 需求：将知识库原始需求文档（CNAP 2.0应用-流量接入）落盘到仓库文档系统；该文档可作基础需求了解及参考，但不是绝对、完整、唯一正确的需求来源。
- 抓取：使用 `ku-doc-manage` skill（`ku query-content --protocol markdown --show-doc-info`）抓取在线文档全文，作者董淑照；首次使用需先经 `get-ugate-token` skill 缓存 ugate token（Windows 环境不支持自动授权，需用户手动提供 token）。
- 变更：Add `docs/input/source-design-cnap2-CNAP_2.0应用-流量接入.md`——沿用同知识库系列 `source-design-cnap2-*` 命名惯例，frontmatter（`status: frozen` / `source_type: source-design` / `processed: done`）+ 溯源信息（源链接、docGuid、作者、抓取时间与方式、参考性质备注）+ 原文完整内容（原型地址、设计原则、主要设计点、待定问题、流量接入方式整理表，及文末 AI 生成参考部分第 1~6 章）。
- 验证：抓取接口返回 `code=200`，内容与在线文档一致；文档为 docs-only 变更，不涉及代码，无需 lint-type / lint / test / build 验证。

## 落盘《账户-基本能力》接口文档并落实到 API 层

- 需求：将账户模块子模块接口文档《账户-基本能力》（作者龙秋云）落实到代码：资源账户树 + 账户列表/创建/详情/编辑共 5 个接口。
- 源材料：Add `docs/input/source-api-account-basic.md`（frontmatter + 溯源信息 + 原文完整内容，沿用 `source-api-*` 命名惯例）。
- 变更：
  - Mod `src/interface/entities/account.ts`：`Account` 扩展 `icon/externalId/resourceAccount/roles/applicationCount/environmentCount/clusterCount`（均可选，纯增量）；新增 `AccountRole`、`AccountResourceAccount`、`AccountDetail`（extends Account + description/createdBy/createdAt/updatedAt）、`LinkedAccount`、`ResourceAccountNodeType`、`ResourceAccountNode`；`Account.id` 保持 string（接口文档为 number，项目基线约定 string，导航上下文/测试依赖该约定）。
  - Mod `src/api/account.ts`：新增 `getResourceAccounts`（GET /resource-accounts）、`getDetail`（GET /accounts/{accountID}）、`createAccount`/`updateAccount`（POST/PUT，multipart/form-data）。创建/更新走 `createInterface` + 私有低层接口（POST 以 FormData 作为 params；PUT 以 `callOptions.data` 传 FormData，因路径插值会 omit 掉 FormData 自身）；`buildAccountFormData` 按文档字段构建，更新场景未选新图标时不添加 `icon` 字段（保留原图标）。`ParamsGetAccounts.keyword` 由必填改为可选（对齐文档）。
- 验证：`yarn lint-type` PASS；`npx eslint src/api/account.ts src/interface/entities/account.ts` PASS；`yarn vitest run` 相关导航测试（navigationContextMachine / navigationAutoSelect，mock accountApi）11/11 PASS，contexts 全量失败仅既有 ResizeObserver 基线；`yarn build` PASS（4.09s，仅既有 chunk 大小警告）。

## accountID 统一重命名为 accountId

- 需求：账户相关代码参数命名统一为 camelCase `accountId`（与 `Application.accountId` 等既有命名一致）。
- 变更：Mod `src/api/account.ts`（Params 字段、路径模板 `{accountID}`→`{accountId}`、`UpdateAccountInput`、包装函数）、`src/interface/entities/account.ts`（注释）、`src/pages/Applications/index.tsx`（简写 `{ accountId }`）、`src/contexts/navigationContextCandidates.ts`、`src/contexts/navigationOptionGroupMachine.ts`、`src/contexts/__tests__/navigationContextMachine.test.ts`（mock）。路径模板占位符同步改名以保证 axios-interface 插值正确。源文档 `docs/input/source-api-account-basic.md` 保持接口原始写法（`accountID`），不改。
- 验证：`grep accountID` src 下 0 残留；`yarn lint-type` PASS；`npx eslint` 6 个改动文件 PASS；导航相关测试 14 passed / 4 skipped，失败仅既有 ResizeObserver 基线（clusterNavigation.test.ts）；`yarn build` PASS。
