# 2026-08-14

## 应用列表/应用模板/应用设置 原始需求文档落盘

Source: 用户提供 ku 知识库需求文档 URL，要求落盘到仓库。

- `docs/input/source-pm-cnap2-applications-templates-settings.md`（新增）：抓取并落盘「CNAP2.0 应用-应用列表 / 应用模板 / 应用设置」原始需求文档（docGuid `MJj-EvC4V4LNKo`，作者张藜千），覆盖三个模块的产品需求：应用列表（筛选/排序置顶/双视图/行内交互）、应用模板（Tab/复制/共享/删除/详情版本）、应用设置（基本信息/成员管理/高级配置-双层删除确认）、模块间联动共 8 节；首次落盘，暂未与前端实现对齐。

验证：

- `ku query-content --protocol markdown` 返回 returnCode 200；落盘正文与抓取文本逐字一致。纯文档落盘，无代码变更，不涉及 lint/test。

## 应用列表需求文档与新原始需求对齐（9 条裁决落地）

Source: 用户对比 `docs/requirements/cnap2-application-list-page.md` 与 `docs/input/source-pm-cnap2-applications-templates-settings.md` 的差异后给出 9 条裁决（冲突以新原始需求为准）。

- `docs/requirements/cnap2-application-list-page.md`：按裁决更新——
  - 筛选：应用分类/环境单选 → 应用类型/环境/用户标签均多选，条件交集、条件内并集（新需求 §5.3）
  - 默认环境：维持 `defaultApplicationEnvironmentId`（裁决 1、5：点卡片/名称/空白处同一逻辑，入口环境一律按该字段）
  - 最近变更：`recentChanges` 首条四段 `·` 拼接，无变更展示 `—`（裁决 8，替代旧"每环境逐条展示"）
  - 排序置顶：由后端处理，列表接口多传"当前应用 ID"，后端插首位（裁决 4）
  - 占位入口：创建应用/从模板创建/应用设置/另存为模板仅渲染按钮 + 占位事件（裁决 2）；其余"应用相关写操作"全部 Out Of Scope
  - 系统标签：保留原措辞（裁决 3）；去掉卡片用户标签点击进筛选（裁决 7）；应用图标无接口字段先占位（裁决 9）
  - Open Questions：新增应用类型四类枚举数值待 PM 补充、排序"当前应用 ID"参数名待接口确认

验证：纯需求文档更新，无代码变更，不涉及 lint/test。

## 应用列表需求按审查裁决收敛

Source: 用户针对子 Agent 需求审查结果给出 13 条裁决。

- `docs/requirements/cnap2-application-list-page.md`：明确应用名称使用 `name`、描述使用 `description`；点击卡片/名称/空白处使用后端已计算的 `defaultApplicationEnvironmentId` 进入工作负载页面，点击具体环境使用 `applicationEnvironmentId`。
- 明确卡片右上角固定渲染收藏、另存为模板、应用设置三个入口，只有收藏实现真实功能，其余绑定占位事件；收藏后不触发重新排序。
- 补充空态：账号无应用展示「占位应用」，有应用但搜索/筛选无结果展示「没有符合筛选条件的应用」；字段缺失统一展示 `—`。
- 明确 `category`、`environmentId`、`labelIds` 多选值均使用英文逗号分隔；`pageSize` 可选 10/20/50/100，默认显式按 10 请求。
- 更新 `curApplicationId` 状态：前端 API 已支持，原始接口文档待同步；请求头注入归接口实现层负责，本需求不约束。
- 为 AI 模型推理应用与 Agent 沙箱应用增加临时占位枚举 `__PENDING_AI_INFERENCE__` / `__PENDING_AGENT_SANDBOX__`，待后端正式值确定后替换。

验证：纯需求文档更新，无代码变更，不涉及 lint/test。

## 应用接口 ID 参数类型统一为字符串

Source: 用户要求同步调整应用接口代码和类型定义，将 ID 从 `number` 改为 `string`。

- `src/api/application.ts`：`ParamsGetApplicationsByAccount.environmentId` 从 `number` 改为 `string`，与 REST 业务 ID 字符串约定及 `src/interface/entities/application.ts` 中的实体字段保持一致。
- 检查应用实体相关 ID：`id`、`accountId`、`applicationEnvironmentId`、`environmentId`、用户标签 `id`、收藏响应 `applicationId` 均已是 `string`，无需重复修改。

验证：

- `yarn lint-type`：通过。
- `yarn test`：通过（38 files / 229 tests）。

## 流量接入需求文档合成（初稿）

Source: 用户要求合成流量接入需求文档，供后续补充与修改。真源：`docs/input/source-design-cnap2-CNAP_2.0应用-流量接入.md`（业务需求，参考）+ `docs/input/source-api-traffic-access.md`（接口契约，冲突时优先）；复用已落地的 `src/api/trafficAccess.ts`（8 接口）与 `src/interface/entities/trafficAccess.ts`（15 实体）。

- `docs/requirements/cnap2-traffic-access.md`（新增）：覆盖功能模块总览（拓扑 / 列表 / 向导 / 抽屉）、接口清单与字段映射、接入类型枚举（10 种，一期 8 种 + 二期 cnap/alb）、命名规则、校验与错误、各类型表单字段与创建逻辑、拓扑节点摘要与连线规则、In Scope（一期切片）/ Out Of Scope、Main User Flows、Business Rules（10 条）、Edge Cases、Open Questions（10 项，含入口命名「服务暴露 vs 流量接入」、环境上下文、一期/二期呈现、编辑交互、删除确认、快速创建、应用级 BNS Group 入口等）、Acceptance Criteria（15 条）。
- 现状核对：现有入口为 `/service-exposure` 占位页（菜单「服务暴露」，contextRequirements 仅 accountId），与流量接入环境级接口（`:appEnvID`）存在上下文缺口，已列入 Open Questions。

验证：纯需求文档落盘，无代码变更，不涉及 lint/test。

## 应用列表 ID 字段类型统一为字符串

Source: 用户指出应用列表相关 ID 字段应统一使用 `String`，要求解释并纠正需求文档中的 `int64` 标注。

- `docs/requirements/cnap2-application-list-page.md`：将 `accountID`、`environmentId` 的字段映射类型统一为 `string`；补充说明原始接口文档参数表仍标注 `int64`，与 REST 响应业务 ID 使用字符串及前端实体模型不一致，需求以字符串约定为准。

验证：纯需求文档更新，无代码变更，不涉及 lint/test。

## 应用列表需求补充接口字段映射

Source: 用户要求结合 `docs/input/source-api-applications.md`、`src/interface/entities/application.ts` 与 `src/api/application.ts`，补充应用列表需求所需数据字段。

- `docs/requirements/cnap2-application-list-page.md`：新增「接口与字段映射」章节，补充应用列表分页接口的路径参数、查询参数、分页响应字段；补充卡片/表格使用的 `id`、`name`、`displayName`、`category`、`description`、`systemLabels`、`userLabels`、`environments`、`defaultApplicationEnvironmentId`、`recentChanges`、`isCollected` 字段映射。
- 补充 `recentChanges` 首条数据的 `changedBy` / `changedAt` / `environmentName` / `changeType` 展示映射，并区分 `applicationEnvironmentId` 与公共环境 `environmentId`。
- 补充 filter-options 的 `applicationCategories` / `environments` / `userLabels` 字段及收藏接口的 `id`、`applicationId`、`isCollected`、`x-baidu-int-username` 关系。
- 明确本需求的 `pageSize=10` 与接口不传时默认 `20` 的差异；标注 `curApplicationId` 已有需求约定但当前接口源文档与 `src/api/application.ts` 尚未同步。

验证：纯需求文档更新，无代码变更，不涉及 lint/test。

## 应用列表表格视图纳入本期实现范围

Source: 用户要求将原始需求文档定义的表格视图纳入 `cnap2-application-list-page.md` 当前实现范围。

- `docs/requirements/cnap2-application-list-page.md`：新增应用表格视图描述，明确列顺序为名称（左固定）/ 类型 / 属性 / 描述 / 环境 / 标签 / 最近生产变更 / 操作（右固定）；补充空值 `—`、中间列滚动、共用筛选/分页/收藏能力及视图切换不重置筛选条件等规则。
- 移除表格视图属于 Out Of Scope 的旧表述，新增表格视图相关用户流程与验收标准；应用设置、另存为应用模板等操作仍保留入口并绑定占位事件。

验证：纯需求文档更新，无代码变更，不涉及 lint/test。

## 应用卡片布局按截图更新

Source: 用户提供应用卡片截图，要求重新描述卡片布局。

- `docs/requirements/cnap2-application-list-page.md`：将卡片布局从左右两部分调整为纵向分层结构：顶部信息区（图标、名称、应用类型、系统标签、右侧操作入口）、描述区、环境区、用户标签区、最近变更区；补充圆角边框、环境 `+N` 折叠、用户标签图标与颜色、最近变更分隔线等截图体现的布局信息。
- 展示信息与验收标准同步更新；复制/另存为模板、应用设置入口保留为占位入口，只有收藏操作在本期实现。

验证：纯需求文档更新，无代码变更，不涉及 lint/test。

## 应用类型枚举值与列表接口 curApplicationId 参数确定

Source: 用户补充两条已确定信息。

- `docs/requirements/cnap2-application-list-page.md`：应用类型枚举值确定两项——`microservice`=云原生微服务应用、`simple_application`=轻量级后端应用（AI 模型推理应用 / Agent 沙箱应用枚举值仍待 PM 补充，保留 Open Questions）；BR11 排序规则更新——列表接口新增可选参数 `curApplicationId`，page=1 时后端用其查询并插入列表第一条，page>1 时忽略；Open Questions 移除"排序参数名待确认"，状态行同步更新。

验证：纯需求文档更新，无代码变更，不涉及 lint/test。
