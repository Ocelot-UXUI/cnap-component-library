# 2026-08-13

## Empty 增强组件实现调整与文档同步

Source: 当前用户需求（Empty 实现调整后的文档对齐，仅同步文档，不改代码/测试）。
Plan: `docs/plans/2026-08-12-design-empty-illustration-plan.md`（closure 后调整，见该 plan 的 Amendment）

- `src/design/Empty/index.tsx`：尺寸样式由内联样式改为 emotion css 类（`!important` 固定插图尺寸）；命中 `imageType` 时仅 `classNames.root` 生效、root 应用纵向居中 flex 布局、图-文间距 S 4px / M·L 12px；未命中分支 `classNames` 不透传。
- `docs/context/conventions.md`：Empty 契约同步上述 `classNames` 限制与布局行为；`docs/design/design-tokens.md` 尺寸档数值不变，无需修改。

验证：

- `yarn lint-type` 通过
- `yarn test src/design/Empty/Empty.test.tsx` 10/18 失败（断言基于旧的尺寸内联样式实现，emotion css 类文本不进入静态 markup）。**未通过，不作为通过基线记录。**

## Empty 单测移除

Source: 当前用户需求（Empty 组件不需要单测）。

- 删除 `src/design/Empty/Empty.test.tsx`（此前 10/18 失败的 18 项用例随文件一并移除），不再为该组件维护单测；上文「测试未同步，列为后续待办」以此为准收口。

验证：`yarn test` 全量通过（37 files / 226 tests）。

## 应用-流量接入接口文档落盘

Source: 用户提供 ku 知识库接口文档 URL，要求落盘到仓库。

- `docs/input/source-api-traffic-access.md`（新增）：抓取并落盘「应用-流量接入（服务暴露）」接口文档（docGuid `BmpsRg9e55_8yB`），覆盖流量接入 CRUD、接入类型、名称预览、流量拓扑、错误响应共 6 节；首次落盘，暂未与前端实现对齐（对齐按 `docs/skills/api-sync-from-ku-prompt.md` 流程另行执行）。

验证：

- `ku query-content --protocol markdown` 返回 returnCode 200；落盘正文与抓取文本逐字一致（10406 chars）。纯文档落盘，无代码变更，不涉及 lint/test。

## 应用接口文档同步（type → category）

Source: `docs/skills/api-sync-from-ku-prompt.md` 流程，重新抓取 ku 在线文档（docGuid `zJ9Wp5AQ1cmFAU`）后同步。

- `docs/input/source-api-applications.md`：抓取时间更新为 2026-08-13，正文替换为最新内容；变更点：列表查询参数 `type` → `category`、响应字段 `type`（如 `MICRO_SERVICE`）→ `category`（如 `microservice`，不再返回应用类型名 type）、filter-options 响应 `applicationTypes` → `applicationCategories`、用途"应用类型"→"应用分类"、接口总览移除"更新账户"条目、`pageSize` 说明更新。
- `src/interface/entities/application.ts`：`Application.type` → `Application.category`（注释同步）；`ApplicationFilterOptions.applicationTypes` → `applicationCategories`。
- `src/api/application.ts`：`ParamsGetApplicationsByAccount.type` → `category`；`pageSize` 注释与 filter-options 注释同步新文档。
- `docs/requirements/cnap2-application-list-page.md`："应用类型"→"应用分类"、`applicationTypes`→`applicationCategories`、`type` 参数→`category` 参数（Goal / 页面结构 B / Main User Flows / Business Rules 5·7 / Acceptance Criteria）。

验证：`yarn lint-type` 通过；`src` 内无 `applicationTypes`/`MICRO_SERVICE`/业务"应用类型"残留（`src/types/application.ts` 为未被引用的遗留文件，不受影响）。

## 账户-基本能力接口文档同步

Source: 用户要求按 `docs/skills/api-sync-from-ku-prompt.md` 流程同步《账户-基本能力》接口文档（docGuid `wDHcvK7WzdAhj5`）。用户确认：文档与接口实现均调整；创建账户路径按接口总览改为 `/account`（上游 3.4 节正文疑为笔误）。

- `docs/input/source-api-account-basic.md`：正文替换为最新全文（7 个接口：`/accounts` 导航栏精简、新增 `/account-summaries`、`/user-account-roles`）；frontmatter 更新抓取时间与 `更新内容` 摘要；创建账户 3.4 节路径按用户决定修正为 `/rest/v1/account`（唯一与上游不一致处，已在 `更新内容` 备注说明）。
- `src/interface/entities/account.ts`：`Account` 精简为 id/name/displayName/icon/externalId（移除 resourceAccount/roles/统计字段）；新增 `AccountDetailResponse`（account 嵌套）、`AccountSummary`、`UserAccountRoles`；`AccountDetail` 保留扁平结构（创建/更新响应用）；账户 ID 类型注释更新（文档已为 string）。
- `src/api/account.ts`：`getDetail` 响应类型改 `AccountDetailResponse`；新增 `getSummaries`（GET /account-summaries）、`getUserAccountRoles`（GET /user-account-roles）；创建路径 `/accounts` → `/account`；`ParamsGetAccounts.keyword` 注释修正（/accounts 仅匹配账户中英文名称，资源账户路径搜索归 /account-summaries）。

验证：

- `yarn lint-type` 通过
- `yarn test` 全量通过（37 files / 226 tests）
- 落盘正文与抓取文本逐字一致（归一化行尾后 7368 chars，仅创建路径一处按用户决定修正）
