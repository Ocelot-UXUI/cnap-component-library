## [2026-08-31] update | 业务页面接入设计系统组件
将应用、账户、集群、环境、设置、示例、更新日志和 BorderGlow 页面中的手写卡片/标题替换为现有 `@/design` 的 `Card` 与 `Typography`，保留原有交互和 AI 语义属性。类型检查通过；全量 lint 仍有既有 `src/utils/i18n` 的 `any` 错误，变更文件定向 lint 通过。

## [2026-08-31] update | 统一业务页面内容区留白
参考本地 CNAP Playground 的 `.content` 规则，在公共 `WorkspacePane` 为普通页面增加桌面端 `28px 36px`、移动端 `20px 16px` 内边距；AI Chat 全屏路径保持无内边距。类型检查与该样式文件 lint 通过。

## [2026-08-31] update | 移除业务内容区外框
按页面走查反馈，移除普通业务页面共用内容框的边框、圆角和阴影，保留页面留白与卡片自身层次；类型检查及布局文件定向 lint 通过。
