# 2026-08-11

## 合成应用列表页面需求文档

- 需求：基于需求评审记录（`docs/input/source-meeting-cnap2-CNAP_2.0应用列表-需求评审记录.md`）与用户补充的页面结构（页头"应用列表"标题 / 新建应用+从模板新建按钮区 / 筛选区 / 卡片-列表展示形态切换），合成"应用列表页面"实现级需求文档；当前切片聚焦**应用卡片展示信息与卡片操作**，不含评审中的删除性结论。
- 分析：抓取评审记录内截图（应用卡片原型，attachId `188dea968d8640cc8697ba8675ba9bca`）经 Windows 内置 OCR（Windows PowerShell 5.1 + WinRT OcrEngine，zh-Hans-CN）提取内容，确认卡片信息项（名称 / 描述 / 环境 / 标签 / 最近变更 / 设置入口）。
- 变更：Add `docs/requirements/cnap2-application-list-page.md`（按 `00-requirement-synthesis-guide.md` 骨架：Goal / 页面整体结构 / In Scope / Out Of Scope / Main User Flows / Business Rules / Edge Cases / Open Questions / Acceptance Criteria）；Mod `docs/requirements/README.md` 增加"应用列表（Applications）"索引项。
- 验证：docs-only 变更，不涉及代码，无需 lint-type / lint / test / build。

## 落盘 CNAP 2.0 应用列表需求评审记录到 docs/input

- 需求：将知识库原始需求评审记录（CNAP 2.0应用列表-需求评审记录，作者董淑照）同步到仓库文档系统，作为应用列表模块需求参考。
- 抓取：使用 `ku-doc-manage` skill（`ku query-content --protocol markdown --show-doc-info`）抓取在线文档全文，docGuid `xEYjeb_5mBzltK`；Windows 本机经 `ku-windows-amd64.exe` + 已缓存 ugate token 直接调用。
- 变更：Add `docs/input/source-meeting-cnap2-CNAP_2.0应用列表-需求评审记录.md`——沿用同知识库系列 `source-meeting-*` 命名惯例（对齐 `source-meeting-cnap2-CNAP_2.0应用-工作负载-需求评审-会议记录.md`），frontmatter（`status: frozen` / `source_type: source-meeting` / `processed: done` / `frozen_reason`）+ 源链接 + 原文完整内容（应用列表、新建应用、应用模板、应用设置四部分需求评审结论）。
- 验证：抓取接口返回 `code=200`，内容与在线文档一致；文档为 docs-only 变更，不涉及代码，无需 lint-type / lint / test / build 验证。

## 落盘《应用》接口文档并新建 application 模块接口

- 需求：将应用模块接口文档（ku docGuid `zJ9Wp5AQ1cmFAU`）落盘到 `docs/input/`，并废弃 `account.ts` 中旧的"获取账号下应用列表"实现，按最新文档在独立 application 模块实现接口层。
- 抓取：使用 `ku-doc-manage` skill（`ku-windows-amd64.exe query-content --doc-id zJ9Wp5AQ1cmFAU --protocol markdown`）抓取在线文档；Windows 下先用 `[Console]::OutputEncoding = UTF8` 修正 PowerShell 对 exe UTF-8 输出的 GBK 误解码（否则中文乱码），token 已缓存（ugate token 永久有效，无需重新认证）。
- 变更：
  - Add `docs/input/source-api-applications.md`：沿用 `source-api-*` 命名惯例 + frontmatter（`status: new` / `processed: pending`）+ 溯源信息（源链接、docGuid、抓取时间与方式）+ 原文完整内容。接口共 1 个：`GET /rest/v1/accounts/{accountID}/applications`（分页响应，含查询参数 keyword/type/environmentId/labelIds/page/pageSize、请求头 x-baidu-int-username、前端跳转规则）。
  - Add `src/interface/entities/application.ts`：`Application` 按文档补全 `type/displayName/description/systemTags/labels/environments/defaultApplicationEnvironmentId/recentChanges/isCollected`（均可选，纯增量）；新增 `ApplicationSystemTag`、`ApplicationLabel`、`ApplicationEnvironment`、`ApplicationRecentChange`、`ApplicationListResult`（分页包装 total/page/pageSize/items）。ID 字段延续项目约定 string（文档为 int64 number，与既有 Account/AppEnvironment 注释口径一致）。
  - Add `src/api/application.ts`：`getApplicationsByAccount`（GET `/accounts/{accountId}/applications`，返回 `ApplicationListResult`）；查询参数 `keyword?/type?/environmentId?/labelIds?/page?/pageSize?`（labelIds 走项目全局 comma 序列化，与文档示例 `labelIds=7&labelIds=8` 的重复 key 写法不同，待后端确认）。
  - Mod `src/api/account.ts`：移除旧的 `getApplicationsByAccount`、`ParamsGetApplicationsByAccount` 及 `Application` 引用（废弃旧实现）。
  - Mod 调用方改走 `applicationApi` 并取 `.items`：`src/pages/Applications/index.tsx`、`src/contexts/navigationContextCandidates.ts`（`flat()` → `flatMap(r => r.items)`）、`src/contexts/navigationOptionGroupMachine.ts`；`src/contexts/navigationContextData.ts`、`src/contexts/navigationSelectorBuilders.ts` 的 `Application` 类型引用改为 `@/interface/entities/application`；`src/test/setup.ts` 与 `src/contexts/__tests__/navigationContextMachine.test.ts` 的 mock 拆分为 application mock 并返回分页结构。
- 验证：`yarn lint-type` PASS；`npx eslint` 10 个改动文件 PASS；`yarn test` 全量 37 files / 226 tests PASS。全量 `yarn lint` 仍报 2 个 error（`src/utils/i18n/index.ts` `no-explicit-any`），为既有预存问题，非本次改动引入。
