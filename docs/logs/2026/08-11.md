# 2026-08-11

## 合成应用列表页面需求文档

- 需求：基于需求评审记录（`docs/input/source-meeting-cnap2-CNAP_2.0应用列表-需求评审记录.md`）与用户补充的页面结构（页头"应用列表"标题 / 新建应用+从模板新建按钮区 / 筛选区 / 卡片-列表展示形态切换），合成"应用列表页面"实现级需求文档；当前切片聚焦**应用卡片展示信息与卡片操作**，不含评审中的删除性结论。
- 分析：抓取评审记录内截图（应用卡片原型，attachId `188dea968d8640cc8697ba8675ba9bca`）经 Windows 内置 OCR（Windows PowerShell 5.1 + WinRT OcrEngine，zh-Hans-CN）提取内容，确认卡片信息项（名称 / 描述 / 环境 / 标签 / 最近变更 / 设置入口）。
- 变更：Add `docs/requirements/cnap2-application-list-page.md`（按 `00-requirement-synthesis-guide.md` 骨架：Goal / 页面整体结构 / In Scope / Out Of Scope / Main User Flows / Business Rules / Edge Cases / Open Questions / Acceptance Criteria）；Mod `docs/requirements/README.md` 增加"应用列表（Applications）"索引项。
- 验证：docs-only 变更，不涉及代码，无需 lint-type / lint / test / build。
- 决策（2026-08-11）：① **B2 悬置**——点击卡片 / 环境项进入应用工作区的落点路由后续切片确定，本切片暂不实现跳转；② **B3 包装层方案**——先获取当前用户名（`window.__icloud__?.username`，必要时异步 `/api/home/v2/login-info`），再作为 `x-baidu-int-username` 请求头写入后调用原接口（需合并公共 headers 避免覆盖 `x-region` 等）。Mod `docs/requirements/cnap2-application-list-page.md`（BR 8、卡片操作 1/2、Open Questions、AC 移除两条跳转验收项）。
- 验证：docs-only 变更，不涉及代码，无需 lint-type / lint / test / build。

## 落盘 CNAP 2.0 应用列表需求评审记录到 docs/input

- 需求：将知识库原始需求评审记录（CNAP 2.0应用列表-需求评审记录，作者董淑照）同步到仓库文档系统，作为应用列表模块需求参考。
- 抓取：使用 `ku-doc-manage` skill（`ku query-content --protocol markdown --show-doc-info`）抓取在线文档全文，docGuid `xEYjeb_5mBzltK`；Windows 本机经 `ku-windows-amd64.exe` + 已缓存 ugate token 直接调用。
- 变更：Add `docs/input/source-meeting-cnap2-CNAP_2.0应用列表-需求评审记录.md`——沿用同知识库系列 `source-meeting-*` 命名惯例（对齐 `source-meeting-cnap2-CNAP_2.0应用-工作负载-需求评审-会议记录.md`），frontmatter（`status: frozen` / `source_type: source-meeting` / `processed: done` / `frozen_reason`）+ 源链接 + 原文完整内容（应用列表、新建应用、应用模板、应用设置四部分需求评审结论）。
- 验证：抓取接口返回 `code=200`，内容与在线文档一致；文档为 docs-only 变更，不涉及代码，无需 lint-type / lint / test / build 验证。

## 落盘《应用》接口文档并新建 application 模块接口

- 需求：将应用模块接口文档（ku docGuid `zJ9Wp5AQ1cmFAU`）落盘到 `docs/input/`，并废弃 `account.ts` 中旧的"获取账号下应用列表"实现，按最新文档在独立 application 模块实现接口层。
- 抓取：使用 `ku-doc-manage` skill（`ku-windows-amd64.exe query-content --doc-id zJ9Wp5AQ1cmFAU --protocol markdown`）抓取在线文档；Windows 下先用 `[Console]::OutputEncoding = UTF8` 修正 PowerShell 对 exe UTF-8 输出的 GBK 误解码（否则中文乱码），token 已缓存（ugate token 永久有效，无需重新认证）。
- 变更：
  - Add `docs/input/source-api-applications.md`：沿用 `source-api-*` 命名惯例 + frontmatter（`status: new` / `processed: pending`）+ 溯源信息（源链接、docGuid、抓取时间与方式）+ 原文完整内容。接口共 1 个：`GET /rest/v1/accounts/{accountID}/applications`（分页响应，含查询参数 keyword/type/environmentId/labelIds/page/pageSize、请求头 x-baidu-int-username、前端跳转规则）。
  - Add `src/interface/entities/application.ts`：`Application` 按文档补全 `type/displayName/description/systemTags/labels/environments/defaultApplicationEnvironmentId/recentChanges/isCollected`（均可选，纯增量）；新增 `ApplicationSystemTag`、`ApplicationLabel`、`ApplicationEnvironment`、`ApplicationRecentChange`、`ApplicationListResult`（分页包装 total/page/pageSize/items）。ID 字段延续项目约定 string（文档为 int64 number，与既有 Account/AppEnvironment 注释口径一致）。
  - Add `src/api/application.ts`：`getApplicationsByAccount`（GET `/accounts/{accountId}/applications`，返回 `ApplicationListResult`）；查询参数 `keyword?/type?/environmentId?/labelIds?/page?/pageSize?`（labelIds 走项目全局 comma 序列化，与文档示例 `labelIds=7&labelIds=8` 的重复 key 写法不同，待后端确认）。
  - Mod `src/api/account.ts`：移除旧的 `getApplicationsByAccount`、`ParamsGetApplicationsByAccount` 及 `Application` 引用（废弃旧实现）。
  - Mod 调用方改走 `applicationApi` 并取 `.items`：`src/pages/Applications/index.tsx`、`src/contexts/navigationContextCandidates.ts`（`flat()` → `flatMap(r => r.items)`）、`src/contexts/navigationOptionGroupMachine.ts`；`src/contexts/navigationContextData.ts`、`src/contexts/navigationSelectorBuilders.ts` 的 `Application` 类型引用改为 `@/interface/entities/application`；`src/test/setup.ts` 与 `src/contexts/__tests__/navigationContextMachine.test.ts` 的 mock 拆分为 application mock 并返回分页结构。
- 验证：`yarn lint-type` PASS；`npx eslint` 10 个改动文件 PASS；`yarn test` 全量 37 files / 226 tests PASS。全量 `yarn lint` 仍报 2 个 error（`src/utils/i18n/index.ts` `no-explicit-any`），为既有预存问题，非本次改动引入。

## 同步《应用》接口文档并更新 application 模块（按 `api-sync-from-ku-prompt`）

- 需求：在线接口文档更新后，按 `docs/skills/api-sync-from-ku-prompt.md` 同步前端（input 文档 + 实体/API 层 + 需求文档）。
- 抓取：`ku-windows-amd64.exe query-content --doc-id zJ9Wp5AQ1cmFAU --protocol markdown`（token 已缓存；`[Console]::OutputEncoding = UTF8` 修正中文乱码）。
- 差异（旧 1 接口 → 新 5 接口）：新增 `filter-options`、收藏、取消收藏 3 个接口；`labelIds` 由 `int64[]`（重复 key）改为 `string`（逗号分隔），与全局 comma 序列化一致，关闭此前"待后端确认"项；响应 `systemTags:[{key,value}]` → `systemLabels:string[]`、`labels` → `userLabels:[{id,name}]`，业务 ID 统一字符串，应用不再返回 createdBy/createdAt/updatedAt；列表接口 `x-baidu-int-username` 必填改为否（收藏接口仍必填）。
- 变更：
  - Mod `docs/input/source-api-applications.md`：正文替换为最新全文（5 个接口），frontmatter 增 `更新内容` 摘要。
  - Mod `src/interface/entities/application.ts`：`Application` 按新文档重写（`systemLabels?: string[]` / `userLabels?: ApplicationUserLabel[]`，删除 `ApplicationSystemTag`，移除旧标签的 accountId/description/createdBy/createdAt/updatedAt）；新增 `ApplicationUserLabel`、`ApplicationFilterOptions`、`ApplicationFilterEnvironment`、`ApplicationCollectionResult`。
  - Mod `src/api/application.ts`：`labelIds` `number[]` → `string`；新增 `getApplicationsFilterOptions`（GET `.../filter-options`）、`collectApplication`（POST `.../collection`）、`uncollectApplication`（DELETE `.../collection`）。
  - Mod `docs/requirements/cnap2-application-list-page.md`：Business Rules 增补筛选数据源（filter-options）、收藏状态（isCollected + collection 幂等）、ID 字符串约定 3 条规则；标签来源规则标注对应 `systemLabels` 字段。
- 验证：`yarn lint-type` PASS；`npx eslint src/api/application.ts src/interface/entities/application.ts` PASS；`yarn test` 全量 37 files / 226 tests PASS。

## 澄清并修订应用列表需求文档（应用卡片切片）

- 需求：针对 `docs/requirements/cnap2-application-list-page.md` 初稿的表述矛盾（搜索范围、状态筛选、系统/用户标签语义、多选、生产环境判定、默认环境、收藏落地、分页）与用户逐轮确认，修订需求文档。
- 确认结论（2026-08-11）：① 搜索统一为按名称（keyword 名称/展示名）；② 筛选去掉"状态"改"应用类型"，类型/环境单选、用户标签多选、各条件可叠加；③ 标签筛选指用户标签；④ 默认环境直接取 `defaultApplicationEnvironmentId`（后端计算，默认生产，前端不做 prod 兜底）；⑤ 卡片布局：左侧头像+名称+系统标签（标签在名称下一行），右侧操作按钮（收藏+设置 icon），左右对齐；⑥ "上次访问记录"落地页逻辑移出，作为独立工作区需求；⑦ 列表视图本切片忽略；⑧ 分页默认 pageSize=10；⑨ 收藏在本切片落地。
- 变更：Mod `docs/requirements/cnap2-application-list-page.md` —— 重写 In Scope（新增"应用卡片布局"、头像、收藏状态与操作）、Business Rules（新增规则 2/5/8/10，更新规则 1/3/4/6，保留 7/9）、Edge Cases（默认环境缺失、收藏失败、分页边界）、Open Questions（生产分组判定、头像占位）与 Acceptance Criteria。
- 假设（已写入文档）："点击标签进筛选"原语义针对卡片标签，但卡片展示的为系统标签（无筛选参数），本切片实现为卡片系统标签仅展示、标签筛选仅在筛选区按用户标签进行。
- 补充确认（2026-08-11）：卡片上同时展示**用户标签**，且点击用户标签可进入按该标签的筛选（与筛选区联动、多选叠加）。Mod `docs/requirements/cnap2-application-list-page.md` —— 卡片布局/展示信息新增用户标签项、卡片操作恢复"点击用户标签进筛选"、Main User Flows 与 Business Rule 5 / AC 补充标签点击联动；"假设"改写为"说明"（系统标签仅展示、用户标签可点击筛选）。
- 补充确认（2026-08-11）：① 卡片环境排序与最近变更按接口返回直出，前端不做分组 / 排序 / 筛选（删除"生产 → 非生产"分组规则、相关 Edge Case 与 Open Question）；② 右上角按钮本切片仅收藏，设置入口移出至 Out Of Scope 留待后续切片。Mod `docs/requirements/cnap2-application-list-page.md`。
- 补充确认（2026-08-11）：应用头像接口暂无字段，先展示占位头像；从 Open Questions 移除头像条目。Mod `docs/requirements/cnap2-application-list-page.md`。
- 子 agent 复审（2026-08-11）：两个并行 General agent 分别审查文档内部一致性与接口可实现性，均无 P0；按用户确认修订（A 组 + B1）——① `defaultApplicationEnvironmentId` 信任后端恒有值，删除对应 Edge Case，BR 2 注明不兜底；② 面包屑 OQ 删除、E 区标注依赖 Draft 文档；③ BR 7 补充公共环境 ID 与卡片应用环境 ID 值域区分、type 原样透传；④ 删除 OQ"无生产环境部署记录"；⑤ BR 8 请求头措辞改"列表建议携带、收藏必填"；⑥ 名称展示 displayName 回退 name、变更时间格式、卡片描述位置、阻止冒泡、环境筛选不收敛卡片、形态切换按钮忽略态、recentChanges 部分缺失展示；⑦ AC 补齐 filter-options 初始化、空态、末页回落、系统标签不可点击。
- 验证：docs-only 变更，不涉及代码，无需 lint-type / lint / test / build。
- 决策（2026-08-11）：① **B2 悬置**——点击卡片 / 环境项进入应用工作区的落点路由后续切片确定，本切片暂不实现跳转；② **B3 包装层方案**——先获取当前用户名（`window.__icloud__?.username`，必要时异步 `/api/home/v2/login-info`），再作为 `x-baidu-int-username` 请求头写入后调用原接口（需合并公共 headers 避免覆盖 `x-region` 等）。Mod `docs/requirements/cnap2-application-list-page.md`（BR 8、卡片操作 1/2、Open Questions、AC 移除两条跳转验收项）。
- 验证：docs-only 变更，不涉及代码，无需 lint-type / lint / test / build。
