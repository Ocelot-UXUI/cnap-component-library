# 2026-06-26

## Log

### 11:55 - 创建 AppLayout 页面级布局边界 follow-up 计划

- 将用户确认的页面级布局边界规则沉淀为独立 plan：语义上明确占据页面区域的组件，其空间布局信息必须外置到 `*Layout` 组件。
- 计划范围聚焦 AppLayout follow-up：Layout 命名、组件私有样式回归 TSX、业务内容通过 slot 渲染到布局组件。
- 保持上一轮 AppLayout 重构已关闭状态，不重开原计划。
- Files: `docs/plans/2026-06-26-app-layout-page-level-layout-boundary-follow-up-plan.md`

### 15:20 - 完成 AppLayout 页面级布局边界 follow-up

- 将 AppLayout 页面级区域组件统一调整为 `*Layout` 命名，并新增 `WorkspaceNavigationMenu` 作为业务菜单组件。
- 将 `WorkspaceSidebar.styles.ts`、`WorkspaceContent.styles.ts` 的私有样式合回对应 TSX；删除独立 styles 文件。
- 将页面级布局边界规则沉淀到 `docs/architecture/module-boundaries.md`。
- 子 agent 关闭审计无代码层 P0/P1；唯一 P1 为计划验证记录未关闭，已补齐。
- Verification: `yarn lint-type` passed; `yarn test` passed; `yarn verify:router-paths` passed; `yarn build` passed; `yarn lint` failed only on unrelated existing issues.
- Files: `src/routers/AppLayout/index.tsx`, `src/routers/AppLayout/WorkspaceSidebarLayout.tsx`, `src/routers/AppLayout/WorkspaceSidebarLogoLayout.tsx`, `src/routers/AppLayout/WorkspaceSidebarCollapseLayout.tsx`, `src/routers/AppLayout/WorkspaceContentLayout.tsx`, `src/routers/AppLayout/FallbackTopNavLayout.tsx`, `src/routers/AppLayout/ICloudTopNavPortalLayout.tsx`, `src/routers/AppLayout/WorkspaceNavigationMenu.tsx`, `docs/architecture/module-boundaries.md`, `docs/plans/2026-06-26-app-layout-page-level-layout-boundary-follow-up-plan.md`

### 15:40 - 放开组件私有样式文件规则并增加校验

- 允许组件目录内使用 `ComponentA.style.ts` 存放当前组件私有样式，要求组件和样式文件位于以组件名命名的目录下。
- 将规则写入 `AGENTS.md` 和 `docs/architecture/module-boundaries.md`，作为 Agent 必须遵守的项目级质量规则。
- 新增 `yarn verify:component-styles`，校验 style 文件目录命名、组件入口存在，以及外部文件不得 import 组件内部 style 文件。
- Verification: `yarn verify:component-styles` passed; `yarn lint-type` passed.
- Files: `AGENTS.md`, `docs/architecture/module-boundaries.md`, `scripts/verify-component-styles.ts`, `package.json`, `docs/plans/2026-06-26-component-style-file-rule-plan.md`
