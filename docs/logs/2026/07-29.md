# 2026-07-29

## Worklog

- `docs/input/source-api-runtime-workloads.md` — 将 Application ID、Environment ID、appEnvID 及相关字段（`applicationID`、`applicationId`、`environmentId`、`appEnvID`、`applicationEnvironmentId`）的类型从 `int64`/`number` 改为 `String`；同步更新 JSON 示例中的数值为字符串。
- `docs/analysis/workload-missing-api-fields.md` — 根据 `source-api-runtime-workloads.md` 2026-07-28 更新，标记 Version、GPU 型号/品牌、Container 子结构为 ✅ 已解决。
- source code migration: 将所有实现代码中 Application ID / Environment ID / appEnvID 的类型从 `number` 改为 `string`。涉及以下文件：
  - 实体接口: `AppEnvironment.id/.applicationId/.environmentId`, `AppEnvironmentCluster.applicationEnvironmentId` (string)
  - API 参数: `applicationEnvironment.ts`, `runtimeResource.ts`, `runtimeOperation.ts`, `rawResource.ts` 中的所有 `appEnvID`/`applicationID` 参数 (string)
  - API 响应处理: 删除 `applicationEnvironment.ts` 中过期的 `Number()` 强制转换
  - 导航上下文: `NavigationContextState`, `NavigationContextReducer`, `OptionGroupMachineContext`, `selectAppEnvID` 返回值
  - 所有 Workloads 页面组件: 修正 `appEnvID` props 类型
  - selector 比较: 添加 `String()` 转换处理 `Account.id (number)` vs `applicationId (string)` 的交叉比较
  - 测试文件: 使用字符串 ID 更新测试数据
  - 验证: `yarn lint-type` 通过（仅剩 3 个 `containers`/`podContainers` 预存错误）；`yarn test` 中核心关联测试通过

## feat: GPU 厂商资源卡片

- 新增 `GpuUsageCard` 通用组件，供 `ResourceUsageView` 展示结构化 GPU 数据：按 `vendor` 映射英伟达、华为昇腾、百度昆仑芯的 Figma 图标，左侧两行展示 `model` 与 `profile`，右侧展示 `x<count>`。接口实际厂商值为 `NVIDIA`、`HUAWEI`、`KUNLUNXIN`。
- `ResourceUsageView` 直接使用 `container.resourceLimits.gpus` 原始数据渲染，不再经 `gpuEntries` 重构，列表以数组下标为 key（只读数据）。`GpuUsageCard` 接收原始 `GpuResource`，按 `vendor`（`NVIDIA`/`HUAWEI`/`KUNLUNXIN`）为卡片选择对应厂商背景图（右上角水印），未知厂商回退通用 GPU 图。修正了初次复制时 ascend 与 kunlunxin 两个厂商背景图错位的问题（华为=红色八瓣、昆仑芯=蓝色）。三份 Figma 资源位于 `src/assets/pod-resource-usage/vendors/`。卡片背景、边框、文字、间距和圆角均使用现有 Design Token，厂商品牌色仅来自导出的图片。

### 验证

- GPU 相关定向测试 5 项、目标文件 ESLint、`git diff --check` 均通过。
- `yarn build` 通过。
- `yarn lint-type` 受既有 ID 类型及 `RuntimeWorkload.containers` 测试类型错误阻断；`yarn test` 为 128 passed / 26 failed / 16 skipped，失败涉及既有 Workloads rows 断言、导航断言、401 与缺少 `ResizeObserver`；`yarn lint` 仍有 `LoadingProgress.tsx`、`date.ts`、`i18n/index.ts` 的既有错误；Design Token 全量检查仍由 `PodContentArea.style.ts` 的既有 `#fff` 失败。

## feat: 容器"上一次终止"区块

- `src/interface/entities/pod.ts` — `Container.lastTermination`（`reason`/`exitCode`/`startedAt`/`finishedAt`）改为可选（无终止记录的容器缺省该对象，符合 K8s lastState.terminated 语义）。
- 新增 `PodDetailDrawer/LastTerminationSection.tsx` — 按需求 4b-5 以两行两列（原因/退出码、开始时间/结束时间）渲染"上一次终止"；仅当 `lastTermination` 含有效记录（reason/startedAt/finishedAt 任一非空）时展示，否则返回 `null`。时间用 dayjs 按 `YYYY.MM.DD HH:mm:ss` 格式化。样式复用 `SectionBar`/`InfoGrid`/`InfoItem`，无硬编码色值。
- `ContainerDetail.tsx` — 在环境变量区块后引入 `<LastTerminationSection>`；将终止逻辑抽离到子组件以控制圈复杂度与文件行数。
- 文档：`docs/requirements/pod-detail-drawer.md` 4b-5 与数据缺口汇总、`docs/analysis/workload-missing-api-fields.md` 第三/四节标记"上一次终止"为 ✅ 已解决。

### 验证

- `yarn lint-type` / `yarn lint`：本次改动文件（`ContainerDetail.tsx`、`LastTerminationSection.tsx`、`pod.ts`）无报错，且原 4 处 `lastTermination` 缺失的测试类型错误已消除；仓库仅剩既有的 ID 类型与 `RuntimeWorkload.containers` 预存错误。
- `yarn test PodContentArea`：10 文件 / 36 用例全部通过。

## feat: 工作负载 / Pod YAML 查看抽屉（plan 2026-07-29-yaml-viewer-drawer）

- 依赖：新增 `@uiw/react-codemirror`、`@codemirror/lang-yaml`、`@codemirror/view`（CodeMirror 6 只读 YAML 查看，React 19 下 smoke 通过）。
- 新增 `PodContentArea/YamlDrawer/`：`YamlViewer.tsx`（CodeMirror 只读封装：语法高亮 + 行号 + 折叠 foldGutter，默认主题）、`index.tsx`（antd Drawer 外壳，宽 980，标题按 `entry` 显示"工作负载 YAML"/"Pod YAML"，归属信息行，底部关闭按钮，四态：加载/成功/失败/空 + resourceType 解析失败）、`resourceType.ts`（`group/version/resource` 解析纯函数，长度 ≠3 判为异常）、`YamlDrawer.style.ts`。
- API：工作负载入口固定走 `getGroupVersionResource`（从 `Workload.resourceType` 解析），Pod 入口固定走 `getCoreResource(resource='pods')`，均 `format='yaml'`。
- 入口接线：`GroupHeader` 补 `onYamlView` 回调 + Dropdown `onClick` 处理 `workload-yaml` 键；`podColumns` 操作列新增前端固定"查看 YAML"图标按钮（`CodeOutlined`，不复用后端 operations 溢出菜单）；`PodGroupTable` 透传 `onYamlView`/`onPodYamlView`。
- Drawer 互斥：`PodContentArea` 将原 `detailPod` 与 YAML 视图态合并为单一 union 状态（`DrawerHost.tsx` 的 `DrawerView`），YamlDrawer 与 PodDetailDrawer 从机制上互斥。
- 工作负载入口 `appName` 本期固定降级为 `-`（`PodContentArea` 无 applicationName 上下文，见 plan Phase 3 Decision）；Pod 入口取 `Pod.applicationName`。
- 本期仅 YAML，JSON 查看留待后续（不装 `@codemirror/lang-json`）；CodeMirror 用默认主题，不接入 Design Token。

### 验证

- `yarn lint-type` 通过；`yarn lint` 新增文件无告警（仓库仅剩 `LoadingProgress.tsx`/`date.ts`/`i18n/index.ts` 既有错误）。
- `yarn test`：新增 `resourceType.test.ts`（4 用例）通过；失败的 5 用例（clusterContext/navigation/executor）与新增改动无关，`git stash` 后基线同样 5 failed，确认为预存失败（ID 类型、401、缺 ResizeObserver）。
- `yarn build` 通过。

### 调整：移除归属信息行 + 新增 YAML 内容搜索高亮

- 依据用户指令，`YamlDrawer` 移除应用/集群/命名空间归属信息行（`OwnershipRow`），`YamlDrawerProps` 删去 `clusterName`/`appName`/`namespace` 显示字段；`yamlTarget.ts` 同步精简。
- 新增依赖 `@codemirror/search`（转为直接依赖，避免依赖 `basicSetup` 隐式传递）。
- `YamlDrawer/index.tsx` 在 YAML 展示区上方新增一行 antd `Input`（`placeholder="搜索 yaml 内容"`、`allowClear`）；切换/重开抽屉自动清空关键字。
- `YamlViewer.tsx` 接收 `keyword` prop，通过 `ref` 拿到 `EditorView`，关键字变化时 dispatch `setSearchQuery.of(new SearchQuery({ search }))`，由 `search()` 扩展高亮全部匹配（`.cm-searchMatch`），空词自动清除。
- 验证：`yarn lint-type` 通过；`yarn lint` 相关文件无告警；`yarn build` 通过（14.48s）。

## style: Pod 列表视图切换器改用 Figma SVG 图标

- 按 Figma「视图切换器」资源，将 `PodContentHeader` Mode Switch 的详细/精简图标从多张 PNG 线条拼接改为单张 SVG：详细=`more-lines.svg`、精简=`less-lines.svg`（均 14×14，位于 `src/assets/pod-content-header/`）。
- `PodContentHeader.tsx` 每个选项渲染单个 `<img>`；`PodContentHeader.style.ts` 中 `ViewIcon` 改为居中 14×14 容器并删除不再使用的 `ViewIconLine`。
- 删除孤立资源 `detailed-line.png`、`simple-line.png`。SVG 颜色随资源内置（#545454），未硬编码到样式；ModeSwitch 尺寸/圆角/背景仍沿用现有 Design Token。

### 验证

- `yarn lint-type` 通过；`yarn lint` 改动文件无告警（仓库仅剩既有 `LoadingProgress.tsx`/`date.ts`/`i18n/index.ts` 错误）。
