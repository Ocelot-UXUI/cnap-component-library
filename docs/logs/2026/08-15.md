# 2026-08-15

## Workloads 多级吸顶滚动（假滚动 / pin-scrub）— plan `docs/plans/2026-08-15-workloads-sticky-scroll-plan.md`

实现假滚动模型：PodContentArea 钉成 WorkloadsHeader 下方的定高窗口，外部 PaneScroll 滚动经进度控制器换算为 Track transform 搬运内容；GroupHeader + 表头由 JS 按进度反向 translate 吸顶交接；分页器按 active 分组底部是否入窗口决定固定窗口底/回流；批量操作栏移到屏幕底部固定。

### 变更

- 新增 `PodContentArea/stickyScroll.ts`：纯逻辑（windowHeight / pinStart / scrubRange / progress / 单组吸顶 translate / activeGroupIndex / paginationPinned）+ `__tests__/stickyScroll.test.ts`（18 用例）。
- 新增 `StickyScrollStage/`：`context.ts`（StickyScrollApi）、`stickyScrollDom.ts`（滚动容器解析、几何量测、transform 施加与复位、监听绑定）、`useStickyScroll.ts`（scroll/rAF + ResizeObserver 量测，effect+cleanup 合规 keep-alive）、`StickyScrollStage.style.ts`、`index.tsx`（Stage/PinnedWindow/Track/Spacer + 窗口底分页插槽）。
- 改 `Workloads/index.tsx`：WorkloadsHeader 保留原生 sticky 并挂 headerRef；Overview 留在窗口外自然滚走；PodContentArea 包进 StickyScrollStage；BatchActionBar 改屏幕底部固定并挂 batchBarRef。
- 改 `PodContentArea/PodGroupTable.tsx`：移除旧 `<Sticky top="56px">`/`<Sticky bottom="0px">`；GroupBlock 注册几何、标 `data-group-block/-id`；GroupHeader 包 `GroupHeaderPin[data-group-header]`；分页固定态经 context + `createPortal` 到窗口底插槽，`PagerRow` 固定 min-height 防跳变。
- 改 `PodContentArea/PodContentArea.style.ts`：GroupBlock 加 position/thead 遮挡样式，新增 GroupHeaderPin / PagerRow。
- 文档：requirement `pod-list-content-area.md` 更新为假滚动模型并订正 :279/:426/:284；project-context 注册本计划；plan status → in progress，各阶段状态与 Phase 5 语义取舍 Decision 落地。

### 验证

- `yarn lint-type`：通过。
- `yarn test`：39 文件 247 用例全绿（含 stickyScroll 18 用例）。
- `yarn build`：通过（vite build 成功；仅既有 chunk >500kB 提示，与本次无关）。
- `yarn lint`：本次改动无新增错误；仅剩既有且无关的 `src/utils/i18n/index.ts` 两处 `no-explicit-any`（本任务未触碰该文件），以及多文件既有 `max-lines` 警告（含新增 `useStickyScroll.ts` 143 行，与仓库既有 9 处同类警告一致，不阻断）。
- `yarn start` 目视 proof（Phase 2–5 的行为验收：窗口 pin/scrub、内容头/筛选条滚走、GroupHeader+表头「一次一个」吸顶交接、固定列/横向滚动同步、分页固定/回流、批量栏置底、keep-alive 恢复、浮层不裁剪）**尚未人工验证**，故计划保持 in progress、未 completed，closure audit 待独立进行。

### 风险/待跟进

- Phase 3 表头（thead）吸顶采用「反向 translate 真实 thead」方案（保留列宽/fixed 列/横向滚动同步的天然优势），但 thead 遮挡层叠、交接连续性、滚动-变换 1 帧延迟抖动仍需 `yarn start` spike 调优——计划标注的高风险项。
- 原生滚动语义（Page Up/Down、浏览器查找定位、focus/选中自动滚动）在定高窗口内丢失，本切片不补偿（见 plan Phase 5 Decision），列入回归。
