# 2026-07-24

## API Sync: 运行时工作负载接口 — Summary resourceRequirements 扁平化

- 抓取来源：ku 知识库 `运行时-工作负载`（doc-id `XwQwllaSaX2yvP`）
- 抓取时间：2026-07-24，方式 `ku query-content --protocol markdown` (via WSL)
- Skill: api-sync-from-ku-prompt

### 变更内容

Summary 接口 `GET /application-environments/:appEnvID/runtime/summary` 的 `resourceRequirements` 由嵌套结构改为扁平字符串结构：

- 旧：`{ cpu: { primary: { value, unit } }, memory: { primary: { value, unit } }, gpu: { primary: { value, unit }, models: [{ model, count }] } }`
- 新：`{ cpu: "32c", memory: "128Gi", gpu: "6" }`

### 修改文件

- `docs/input/source-api-runtime-workloads.md` — 更新 frontmatter（抓取时间、更新内容）与 Summary 响应示例为扁平结构
- `src/interface/entities/runtimeSummary.ts` — 删除 `ResourcePrimary` / `ResourceSummaryItem` / `GpuResourceSummary` / `GpuModel` 四个接口；新增 `ResourceRequirements` 接口；将 `RuntimeSummary.resourceRequirements` 改为 `ResourceRequirements` 类型

### 未改动

- `src/api/runtimeResource.ts` — `getRuntimeSummary` 路径与参数无变化，无需修改
- 无组件消费此 API、无 mock 数据，无下游影响

### 验证

- `yarn lint-type` — 通过（exit 0）

## 需求文档更新：纵向扩缩弹窗（vertical-scale-dialog）

- 文件：`docs/requirements/vertical-scale-dialog.md`
- 变更内容：
  - 新增存储（ephemeralStorage）资源项，表格列、数据来源基线、请求体示例、校验、验收标准均同步加入存储
  - 资源单位枚举明确为：CPU → c/nc；内存 → Mi/Gi/Ti；存储 → Gi
  - 新增值与单位分割/拼接规则：接口返回的"数字+单位"字符串需分割为数值与单位展示，提交时再拼接回字符串
  - 集群选中态驱动资源项可用性：未选中集群时整行资源项及 Limit 复选框禁用；选中集群时资源项启用且 Limit 自动勾选；取消集群选中时 Limit 同步 Req 并取消勾选
  - 修正触发接口请求体结构：`resourceLimits`/`resourceRequests` 由 `verticalScale()` 内部置于 `targets[].params` 下（对齐 `src/api/runtimeOperation.ts:138` 与 `docs/input/source-api-runtime-workloads.md` 真源）
- Open Questions 新增一条非阻塞：接口返回资源值单位不在枚举内时如何处理
- 未改代码，无需运行验证命令

## 需求文档新增：批量操作栏（batch-action-bar）

- 文件：`docs/requirements/batch-action-bar.md`（新建）
- 父需求：`docs/requirements/workloads-page.md`（子需求 3 — H 区域业务逻辑）
- 来源：Figma Frame 824 + 用户指令
- 覆盖内容：
  - 显隐机制（保留现有 AnimatePresence + portal 机制）、跨 group 选中计数
  - 5 个批量操作集合（删除重建 / 重启 / 屏蔽 / 解除屏蔽 / 强制删除）与 capability 映射
  - 按钮可用性聚合规则：任一所选 Pod 的对应操作 `disabled=true` 或缺少该操作 → 批量按钮置灰
  - hover 置灰按钮 Tooltip 显示禁用原因
  - 强制删除为危险操作（红色文字 + 分隔线）
  - 视觉规范引用 design tokens（`semantic.button.primary.bg` 等）
- 同步更新 `workloads-page.md` 子需求 3 的 H 区域指向
- Open Questions：多 Pod 禁用原因聚合方式、屏蔽/解除屏蔽弹窗、强制删除是否复用删除重建弹窗、Figma 与操作集合差异

## 需求文档修订：批量操作栏 — 固定按钮集

- 文件：`docs/requirements/batch-action-bar.md`
- 修订内容：移除 `supportsBatch` 动态过滤机制，改为将 5 个批量操作按钮按固定顺序写死在栏中；Pod `operations` 仅用于决定每个按钮的启用/禁用态，不参与按钮渲染或排序
- 原因：依赖动态过滤会导致按钮顺序随 Pod 数据变化而不确定，固定写死可保证顺序稳定
- 同步更新 Goal / In Scope / 业务规则 3、4 / 验收标准 / Implementation Notes
- 未改代码，无需运行验证命令

## 需求文档补充：强制删除弹窗独立文档

- 新建：`docs/requirements/batch-pod-force-delete-dialog.md`（批量强制删除 Pod 弹窗，独立文档）
- 撤回：从 `batch-pod-delete-rebuild-dialog.md` 移除之前加入的「强制删除变体」章节
- 内容：独立文档引用删除重建弹窗，描述二者差异（标题/功能说明/操作提示文案/提交接口 `pod.delete-force`/成功提示），其余一致；不约束组件实现方式（倾向复制删除重建弹窗代码后修改，而非同组件参数区分）
- `batch-action-bar.md` 解耦：能力映射表/Open Question/验收标准/操作→弹窗映射不再关心强制删除弹窗是否复用删除重建弹窗，仅指向独立文档
- `workloads-page.md` 子需求 3 补充强制删除弹窗指向
- 未改代码，无需运行验证命令

## workload 需求文档一致性审计修复（批量）

对全部 workload 需求文档做交叉审计后修复，仅文档变更、未改代码。

### 字段/数据来源修正

- `pod-list-content-area.md`：版本 hover 字段 `serverVersion` → `currentVersion`；作废"缺失 clusterName"的过期 Open Question（接口已返回）。
- `pod-detail-drawer.md`：事件时间字段 `lastTimestamp` → `lastSeen`。
- 删除 `app-restart-modal` / `horizontal-scale-dialog` / `vertical-scale-dialog` 中 `application.restart` / `application.scale-h` / `application.scale-v` 等 operation name 描述，改为仅依赖封装好的 RESTful 接口。

### 数据缺口标注（能用则用 + 占位）

- `pod-detail-drawer.md`：端口/挂载/环境变量/上一次终止/GPU 型号/版本/暴露 未定义或未返回字段改占位并标注缺口，新增「数据缺口汇总」表。
- `delete-deployment-resource-dialog.md`：接口缺失，标注"暂不实现（阻塞）"，修正 trigger 路径为含 `:operation`。

### 术语与一致性

- 统一"摘流/接流"为"屏蔽/解除屏蔽"：「已摘流」→「已屏蔽」，「屏蔽与接流」→「屏蔽与解除屏蔽」。
- `workloads-page.md`：H 区改为引用 `batch-action-bar.md`，不再重复子需求内容；补父需求层文档定位/Roles/Open Questions/Acceptance。
- 重启次数颜色阈值消除断档：≥6 红 / 3-5 橙 / ≤2 黑（pod-list 与 pod-detail 同步）。

### 阻塞项处理

- `horizontal-scale-dialog.md`：3 条阻塞项确认——期望副本数默认=当前副本数、无上限（≥1 正整数）、异步操作（订单生成即成功）。
- "查看执行详情"链接在 4 篇弹窗文档降级为非阻塞，先以文字占位。

### 规范性

- `cnap2-cluster-selector.md`：补规范映射与 Roles/Permissions 说明。
- 为缺状态行的操作弹窗文档补 `状态`。

### 产出

- 新增 `docs/analysis/workload-missing-api-fields.md`：缺失接口字段汇总，供后端接口调整参考。

验证：本次仅文档变更，未运行 lint/build。

## 新增架构文档：Workload 领域数据模型

- 新建 `docs/architecture/workload-domain-model.md`（标注"目标设计，待分阶段实现"）。

### 内容

- 现状复杂度诊断：资源三套表示 / Operation 重复 / Workload 两套实体 / 裸 string 状态 / 操作数据链与联动散落。
- 领域概念与聚合边界：Context 坐标、WorkloadGroup 与 Pod 两条投影、Operation→Order、Summary 投影。
- 五层模型：RuntimeScope / 值对象（ResourceRef·ResourceSpec+Quantity·PodStatusDescriptor·Version）/ 聚合 / Capability Registry / 读写分离 + Session 状态机。
- 关系图、类型草图、DTO↔Domain 映射、目录结构建议、落地优先级、Open Questions。

### 同步

- 更新 `docs/architecture/README.md` Owner Docs 索引。
- 未改代码，无需运行验证命令。

## 新增计划：Workload 领域模型（纵向扩缩试点）

- 新建 `docs/plans/2026-07-24-workload-domain-model-vertical-scale-pilot-plan.md`，Plan Status: proposed，Owner: v_wangkaiyuan02。
- 活基线核对：纵向扩缩弹窗尚未实现（`src/pages/Workloads/` 仅子需求 1 壳子），`src/domain/` 不存在 → 试点为首次实现（非改造）。
- 结果面：建立 Domain 层最小基础件（ResourceRef / ResourceSpec+Quantity / adapter / 能力注册表雏形），并以纵向扩缩弹窗跑通 loader + XState machine + build 编排范式。
- 分 5 阶段（Domain 落位 → 值对象/适配器 → 能力注册表+组装 → 弹窗编排+UI → 验证），含 Closure Gates 与风险。
- 同步更新 `docs/plans/README.md` Active Plans。
- 未改代码；plan 为 proposed，待独立 draft review 收敛为 planned 后方可实现。

## 架构文档补充：L2 读模型骨架细化

- `docs/architecture/workload-domain-model.md` L2 小节扩写：补"两聚合根 + 软关联（非单一深树）"的理由，以及两套 Workload 合并/ResourceRef 身份/内嵌值对象/与 DTO 分离/层边界等关键决策与依据。
- 未改代码。

## Plan 独立审计 + 修订收敛为 planned

- 用 General subagent 对 `2026-07-24-workload-domain-model-vertical-scale-pilot-plan.md` 做独立 draft review：基线核对基本属实，发现 1 项 Blocking（B1：Phase 4 触发入口范围/证明缺口）+ 建议 N2（domain 分层评审门）/N4（需求仍澄清中风险）。
- 按用户决策（方案 A，纳入范围）修订：
  - `docs/requirements/workloads-page.md`：A 区域"全部工作负载"下拉改名 **WorkloadGroupSelector**，新增「A 区域功能细化」——数据源 `runtime/groups`、受 `clusterId` 影响、单选默认"全部工作负载"、选中值供 Workload 级弹窗带入；标题栏操作**渲染 `targetKind` 为 `None`/`Workload`**、点击按 capability 路由。
  - plan：Goals/Phase 4 纳入触发入口改造与 WorkloadGroupSelector；Phase 1 domain 分层升级为评审门（draft review 已通过）；补 N4 产品风险；基线补充 Header 现状措辞；Plan Status `proposed → planned`，新增 Draft Review 元数据。
  - 同步 `docs/plans/README.md` Active Plans 状态为 planned。
- 未改代码。

## 实现：Workload 领域模型试点 Phase 1–3

- **Phase 1**：`docs/architecture/module-boundaries.md` 补入 `domain/` 层（Module Map + 依赖规则 + 禁止项：domain 纯逻辑，仅依赖 interface/entities、types、utils，不依赖 React/组件/api/contexts）。
- **Phase 2**：新增 `src/domain/workload/`：
  - `model.ts`（`ResourceRef` / `CommandTarget` / `OperationCommand`）
  - `resource.ts`（`Quantity` / `ResourceSpec` + parse/format/单位拆拼、`toBaseValue`/`isLimitGteRequest` 单位换算与降级、`toResourceSpec`/`fromResourceSpec`）
  - `adapters.ts`（`RuntimeWorkload`→`ResourceRef`、容器 `Record<string,string>`→`ResourceSpec`）
  - `index.ts` 公共入口
- **Phase 3**：`capability.ts`（`capabilityRegistry` 仅注册 VerticalScale；`buildVerticalScaleCommand` 纯函数产出 `OperationCommand`，resourceLimits/Requests 序列化入 per-target params，页面层再映射为 `verticalScale()` 入参）。
- 测试：`__tests__/resource.test.ts`(13) + `__tests__/capability.test.ts`(3) 全通过。
- 验证：`yarn test src/domain/workload` 16 项通过；`yarn lint-type` 通过（exit 0）。`yarn lint`：新增 domain 文件无告警，但仓库存在**既有**（非本次引入）lint 错误（navigationOptionGroupMachine、ResourceLimit、date.ts、i18n 等 max-lines/any/eqeqeq），不作为本次通过基线。
- Plan 状态：`planned → in progress`；Phase 1–3 标记 done。Phase 4/5 待续。

## 实现：Workload 领域模型试点 Phase 4–5

- **Phase 4（编排 + UI）** 新增 `src/pages/Workloads/operations/verticalScale/`：
  - `rows.ts`（联动纯逻辑：buildRows/toggleCluster/toggleLimit/editField/校验/映射）
  - `loader.ts`（getWorkloadGroups → getRuntimeWorkloads → 聚合容器）
  - `machine.ts`（XState v5：顺序拉取 + 联动，委托 rows.ts）
  - `selectors.ts`（canSubmit / 底部提示）、`submit.ts`（OperationCommand → verticalScale 入参）
  - `VerticalScaleModal/`（index + ClusterTable + ResourceCell + style）
- **Header 触发入口**：`ScopeSelector`→`WorkloadGroupSelector` 接入 `getWorkloadGroups()`；操作渲染由 `None` 扩展为 `None`/`Workload`；"纵向扩缩"点击打开弹窗并带入已选 group。
- **Phase 5 验证**：`yarn lint-type` 通过；`yarn build` 通过；新增 26 单测通过（domain 16 + rows 10）；新增文件 `npx eslint` 无告警。
- **已知依赖降级**：`NavigationContext` 无 `clusterId` 且不暴露 `appEnvID`（页面暂用占位 `appEnvID=0`）——WorkloadGroupSelector"随集群刷新"与端到端联调延后至 cluster-selector + 子需求4；当前不带 clusterId（=全部集群）。
- **既有失败（非本次回归）**：`src/navigation/__tests__/derive.test.ts > generates internal menu paths without basename` 在 stash 后的干净基线即失败，与本改动无关。
- Plan 状态：`in progress → partially completed`；Phase 4/5 标记 done，Closure Gates 除独立关闭审计外均满足。

## 起草计划：集群选择器 + appEnvID 解析

- 调研发现：**appEnvID 并非缺口**——`NavigationContextSnapshot` 已暴露 `availableEnvironments: AppEnvironment[]`（`id` 即 appEnvID），按 `environmentId` 匹配即可解析；`WorkloadsHeader` 仅是尚未接线（占位 0）。故不另立 appEnvID 澄清文档，将其作为 selector 折入计划 Phase 1。
- clusterId：需求 `cnap2-cluster-selector.md`（Draft）已定义第四维契约，但**无计划**。
- 新建 `docs/plans/2026-07-25-cnap2-cluster-selector-plan.md`（Plan Status: proposed）：Phase 1 appEnvID selector → Phase 2 clusterId 契约扩展（评审门）→ Phase 3 状态机集群区域 → Phase 4 面包屑集群选择器 → Phase 5 路由需求 + 接线关闭纵向扩缩 pilot Follow-up。含 Closure Gates 与需求门/评审门风险。
- 同步 `docs/plans/README.md` Active Plans。
- 未改代码；plan 为 proposed，待 requirement 定稿 + 独立 draft review。

## 集群选择器 plan：独立 draft review + 修订收敛

- 用 General subagent 对 `2026-07-25-cnap2-cluster-selector-plan.md` 做独立审计：基线核实基本属实（appEnvID 可由快照 `availableEnvironments` 派生属实）；发现 2 项 Blocking——B1 需求仍 Draft、B2 Phase 2 Decision 缺备选/剩余风险；以及 N1-N5 准确性/措辞项。
- 修订：
  - 需求 `cnap2-cluster-selector.md` 状态 Draft → **已评审定稿（2026-07-25）**（用户已确认审查通过，闭合 B1）。
  - plan：Phase 2 Decision 补备选方案与剩余风险（B2）；Baseline 补"工作负载路由现缺 environmentId""集群未纳入面包屑三维 plan 范围"；Phase 3 措辞更正为"`environmentChanged` 通知通道 + appEnvID 注入为新增"；Goals 限定 pilot Follow-up 关闭范围为 appEnvID/clusterId；Phase 4 注明 hex→semantic token 映射；Risks 需求门标记已满足。
  - Plan Status `proposed → planned`，新增 Draft Review 元数据（分阶段授权：P1-2 可启动，P3-5 gated 于评审门）；同步 README。
- 未改代码。
