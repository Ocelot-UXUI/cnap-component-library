# 2026-07-02

- 创建 CNAP 2.0 应用外壳和导航交互实现计划草案 `docs/plans/2026-07-02-cnap2-application-layout-navigation-plan.md`，覆盖本地 Header 占位、一级导航溢出、更多弹层、二级导航折叠、内容区间距、页面分类子路由和验证门控；当前状态 proposed，需独立 draft review 后开始实现。
- 根据选中 Figma 稿 `小屏更多，点击账户，选中二级` 启动计划实现，将计划状态更新为 in progress；落地 56px 本地 Header 占位、60px 一级导航、200px/60px 二级导航、32px 内容区间距、More 溢出弹层和 LocalStorage replacement 持久化。
- 拆分 AppLayout 导航壳实现：新增一级导航、二级导航、样式和存储辅助文件，避免巨型单文件；`yarn lint-type`、`yarn test`、`yarn build` 通过，`yarn lint` 仅剩既有无关问题。
- 根据 subagent 审查结果修复除 ResizeObserver 兼容性外的问题：修正内容区 fixed top 覆盖 Header、LocalStorage 读取校验与写入异常保护、导航壳行数、Header 内容区内联 style、导入顺序和行长；`yarn lint-type`、`yarn test`、`yarn build` 通过。
- 修正一级导航 More 和辅助入口交互：More 仅在存在隐藏业务入口时渲染，并改为 hover 弹出；底部辅助入口增加 Tooltip；`yarn lint-type`、`yarn test`、`yarn build` 通过。
- 清理 `src/constants/layout.ts` 中旧布局兼容命名，将剩余引用迁移到 `SECONDARY_SIDEBAR_WIDTH`、`SECONDARY_SIDEBAR_COLLAPSED_WIDTH` 和 `WORKSPACE_CONTENT_GUTTER`；`yarn lint-type` 通过。
- 重构 AppLayout 导航组件边界：将 `WorkspaceNavigationShell` 更名为 `WorkspaceNavigationLayout`，把一级导航拆为 `WorkspacePrimaryNavigationContainer` 与 `WorkspacePrimaryNavigationLayout`，二级导航更名为 `WorkspaceSecondaryNavigationLayout`，并补充布局可嵌套且布局组件以 `Layout` 结尾的设计规则；`yarn lint-type`、`yarn test`、`yarn build` 通过。
- 将布局组件边界规则从 Comate 私有 memory 迁入 AGE 真源：项目通用规则归档到 `docs/context/conventions.md`，导航模块边界归档到 `docs/architecture/navigation-system.md`，并删除重复 memory 记录；`yarn lint-type` 通过。
- 整理 `src/routers/AppLayout/` 目录结构：顶部导航归入 `topNavigation/`，工作区布局归入 `workspace/layout/`，一级/二级导航归入 `workspace/navigation/primary/` 与 `workspace/navigation/secondary/`；排查确认旧侧边栏兼容组件未被引用后删除 `workspace/legacySidebar/` 内容；`yarn lint-type`、`yarn test`、`yarn build` 通过。
- 将 AppLayout 中承担区域布局职责的容器从 `@emotion/css` class 改为 `@emotion/styled` 组件，包括应用根布局、顶部导航 Layout、工作区布局、内容区 Layout、一级导航和二级导航布局容器；保留按钮状态和 AntD overlay 等 class 组合场景；`yarn lint-type` 通过。
