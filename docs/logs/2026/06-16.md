# 2026-06-16

## REQ-AI-14 路由信息暴露给 LLM

实现完成。变更文件:

- `src/routes/create.ts` — Route 接口增加 path 字段，新增 RouteMeta 类型
- `src/routes/index.ts` — 新增 getRouteMetas()、formatRouteContext()
- `src/api/ai/tools.ts` — 重写为 navigate 工具 schema，routeKey.enum 从路由动态生成
- `src/api/ai/types.ts` — 新增 AiTool 类型
- `src/executor/agentLoop.ts` — 新增 executeNavigate()、handleToolCalls()，navigate 直接执行不走 execute
- `src/executor/AIExecutorProvider.tsx` — 移除 capabilityMap 引用，executeAction 简化为 DOM 操作

capabilities 层零引用验证通过（仅 capabilities/index.ts 内部自引用）。

验证: yarn lint-type ✓, yarn build ✓, yarn lint（无新增错误）

## REQ-AI-14 Closure Audit (3 rounds)

Round 1: 两方独立审计发现 2 个 FAIL — formatRouteContext() 未被调用（死代码）、formatRouteContext() 格式化缺陷（无 description 的路由输出畸形）
Fix: useAIChatLogic.ts 接入 formatRouteContext()，修复格式化逻辑
Round 2: 验证修复正确，所有 5 项检查 PASS
Round 3: 端到端一致性审计，三条数据流（system prompt 注入、tool schema enum 生成、navigate 执行链）全部 PASS，无循环依赖、无类型不匹配、capabilities 零引用

## REQ-AI-14 已关闭

用户手动验证 navigate 工具调用通过，需求关闭。

## Bug Fix: SSE tool_calls 流式解析丢失字段

修复 3 个关联问题：

1. `parseSseStream` 覆盖赋值导致 tool_calls 字段（id/name/type）丢失 → 改为 Map 按 index 累积
2. `ChatMessage` 字段名 camelCase 与 API snake_case 不匹配 → 字段名改为 snake_case
3. `handleToolCalls` error 分支缺少 assistant message 配对 → 补齐

详见 `docs/bugs/01-sse-tool-calls-streaming-accumulation.md`
验证: yarn lint-type ✓, yarn build ✓
