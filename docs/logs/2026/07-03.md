# 2026-07-03

- 基于 Figma 节点“面包屑/账户下拉”“面包屑/应用下拉”“面包屑/环境下拉”“其他细节”和现有导航文档，新增 `docs/requirements/cnap2-breadcrumb-context-selectors.md`，沉淀 CNAP 2.0 顶部面包屑账号/应用/环境选择器的 UI 与交互需求；本次仅做需求合成，未做代码实现。
- 更新 `docs/requirements/cnap2-breadcrumb-context-selectors.md`：确认环境搜索 placeholder 为“请输入环境名称”，行内快捷图标先按 Figma 占位但不绑定语义，当前阶段不考虑云端 Header，并补充面包屑容器右侧 60px 间距、子项最大宽度 `容器实际宽度 / 子项数量 * 1.2` 和中间省略规则。
- 继续收敛 `docs/requirements/cnap2-breadcrumb-context-selectors.md` 的 MVP 规则：补充最小数据模型、AntD Select 自定义下拉实现基线、上级维度切换后清空下级选中和待选列表、防抖本地 contains 搜索、接口一次性返回 Tab 分组数据、空 Tab 使用 AntD Empty、Footer 与行内快捷图标仅作无事件占位。
- 新增 `docs/plans/2026-07-03-cnap2-breadcrumb-context-selectors-plan.md`，规划 CNAP 2.0 面包屑账号/应用/环境选择器实现；计划明确对外只暴露 `BreadcrumbContextSelectors`，调用方不感知内部 layout/业务拆分，模块内部通过 hooks、纯函数和内部 layout-oriented helpers 保持职责边界。
- 开始实现 CNAP 2.0 面包屑账号/应用/环境选择器：新增 `src/routers/AppLayout/topNavigation/breadcrumb/` 模块，提供 `BreadcrumbContextSelectors` 对外组件、最小数据适配、Select 自定义下拉、Tabs、搜索、Empty、hover 占位快捷图标、Footer 占位按钮和面包屑宽度计算；替换 `TopNavContent` 中旧的静态上下文选择器；`yarn lint-type`、`yarn test`、`yarn build` 通过。
- 更新本地开发服务器配置：`vite.config.ts` 显式设置 `server.strictPort = false`，保留从 3000 端口启动并在端口占用时自动递增重试的 Vite 行为；`yarn lint-type` 通过。
- 调整面包屑选择器数据边界：将收藏、最近访问、环境分类等接口分组语义下沉到 `src/api/navigationContext/`，breadcrumb 模块改为调用 selector option group API 并仅做 UI 图标适配；同步更新 `docs/context/conventions.md` 中 API 与数据源边界规则。
- 修正导航上下文 API 数据命名：产品运行时代码中写死的候选数据统一称为 static data，不再使用“静态 mock 数据”概念；`src/api/navigationContext/mockData.ts` 更名为 `staticData.ts`。
