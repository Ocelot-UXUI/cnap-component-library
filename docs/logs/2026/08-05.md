# 2026-08-05

## 端口复制改用 antd Typography + SectionBar 支持 title prop

- 端口表格两处复制改用 antd `Typography.Text` 的 `copyable`（内部同样走 copy-to-clipboard，符合剪贴板规范，且自带"已复制"图标/Tooltip 反馈）：
  - `detailTables.tsx`：`PortsTable` 操作列每行改为 `Typography.Text copyable`（复制 `IP:PORT`）；无 Pod IP 时降级为禁用色 `CopyOutlined` + Tooltip。删除不再使用的 `copyPortAddresses`、`copyText`/`message`/`Button` 引用。
  - `ContainerDetail.tsx`：`复制全部 IP:PORT` 改为 `Typography.Text copyable`（复制换行拼接的全部地址）。
- `SectionBar` 由 styled.div 改为组件（`SectionBar.tsx`）：标题+数据作为 `title` prop 传入，内部用 `SectionBarTitle`（`flex:1`）包裹，右侧放操作（children）。styled 拆为 `SectionBarRoot` + `SectionBarTitle`，移除已失效的 `.ant-5-btn:last-child` 规则。`ContainerDetail.tsx` / `LastTerminationSection.tsx` 全部 5 处 SectionBar 改为 `title` 传参。
- 验证：`yarn lint-type` 通过（0 error）；`yarn lint` 针对改动文件 0 error/0 warning（仓库其余 4 error+6 warning 均在未改动文件的既有基线中）。
- 变更未提交，留待用户决定。

## 集群下拉「集群名称」前置集群类型 icon

- 需求：`ClusterDropdown` 表格「集群名称」列在集群名前渲染 `clusterConnector` 对应类型 icon（icon 已置于 `src/assets/illustrations/`）。
- 变更（仅 breadcrumb 自有文件）：
  - `clusterConnectorIcons.ts`（新增）：import 9 个 illustration svg（作为 URL），维护 `clusterConnector`（大写归一）→ icon URL 映射，导出 `getClusterConnectorIcon`。映射：EKS-CCE→eks-cce、EKS-ECI→eks-eci、EKS-FED→eks-fed、SCI→eks-sci、BCE-CCE→bce、AWS-EKS→aws、GCP-GKE→gcp、K8S→k8s、K0S→k0s。
  - `ClusterDropdown.styles.ts`：新增 `ClusterNameCell`（flex + `spacing.xs` 间距，`.cluster-connector-icon` 16×16 不收缩）。
  - `ClusterDropdown.tsx`：`clusterName` 列 render 改为 `<ClusterNameCell>` 内 icon（命中才渲染，多彩插画走 `<img>`）+ 集群名。
- 备注：未知类型不渲染 icon（仅显示名称）；SCI 因无独立 `sci.svg`，映射到 `eks-sci.svg`（最近匹配），如有专属图标可再调整。
- 验证：`yarn lint-type` 通过（0 error）；`yarn lint` 三个改动文件 0 error/0 warning（仓库其余 4 error+6 warning 均在未改动文件的既有基线中）。
- 变更未提交，留待用户决定。



## 统一剪贴板复制：copy-to-clipboard 替代 navigator.clipboard

- 借鉴 antd `Typography` 的 copyable 实现，新增项目级规范：复制能力统一走 `@/utils/clipboard` 的 `copyText`，禁止业务代码直接用 `navigator.clipboard`。
- 变更：
  - `package.json`：新增依赖 `copy-to-clipboard@^4.0.2`（antd 本已传递依赖）。
  - `src/utils/clipboard.ts`（新增）：`copyText(text, options?)` 委托 `copy-to-clipboard`，**返回 `Promise<boolean>`**（v4 的 `copy()` 异步：优先 `navigator.clipboard`，失败回退 `execCommand`）；支持 `{ format: 'text/html' }`。
  - `src/utils/__tests__/clipboard.test.ts`（新增）：mock `copy-to-clipboard`，覆盖 delegate / format 透传 / 失败 resolve false（均 async）。
  - 改造 3 处调用点：`PodDetailDrawer/detailTables.tsx`（`copyPortAddresses` 改 async）、`Settings/ApiKeysTab.tsx`、`AIChat/MessageActions.tsx`，均 `await copyText(...)`；反馈 `message` 由各调用方自持。
  - `docs/context/conventions.md`：新增 "Clipboard / 复制到剪贴板" 规范条目（含理由与"必须 await、勿用 `if(copyText())`"提醒）。
- 理由：`navigator.clipboard` 仅安全上下文可用，HTTP/iframe/旧浏览器/内网会静默失败；`copy-to-clipboard` v4 带 `execCommand` 回退更稳。注意 v4 `copy()` 已是异步返回 `Promise<boolean>`（v3 为同步 `boolean`），调用方必须 `await`。
- 验证：见本条末尾（依赖未安装，`node_modules` 缺失时补记）。
- 变更未提交，留待用户决定。

## 集群下拉「集群名称」前置集群类型 icon

- 需求：`ClusterDropdown` 表格「集群名称」列在集群名前渲染 `clusterConnector` 对应类型 icon（icon 已置于 `src/assets/illustrations/`）。
- 变更（仅 breadcrumb 自有文件）：
  - `clusterConnectorIcons.ts`（新增）：import 9 个 illustration svg（作为 URL），维护 `clusterConnector`（大写归一）→ icon URL 映射，导出 `getClusterConnectorIcon`。映射：EKS-CCE→eks-cce、EKS-ECI→eks-eci、EKS-FED→eks-fed、SCI→eks-sci、BCE-CCE→bce、AWS-EKS→aws、GCP-GKE→gcp、K8S→k8s、K0S→k0s。
  - `ClusterDropdown.styles.ts`：新增 `ClusterNameCell`（flex + `spacing.xs` 间距，`.cluster-connector-icon` 16×16 不收缩）。
  - `ClusterDropdown.tsx`：`clusterName` 列 render 改为 `<ClusterNameCell>` 内 icon（命中才渲染，多彩插画走 `<img>`）+ 集群名。
- 备注：未知类型不渲染 icon（仅显示名称）；SCI 因无独立 `sci.svg`，映射到 `eks-sci.svg`（最近匹配），如有专属图标可再调整。
- 验证：`yarn lint-type` 通过（0 error）；`yarn lint` 三个改动文件 0 error/0 warning（仓库其余 4 error+6 warning 均在未改动文件的既有基线中）。
- 变更未提交，留待用户决定。



## 横向扩缩去除容器选择器 + 内联工作负载选择

- Plan：`docs/plans/2026-08-05-horizontal-scale-remove-container-selector-plan.md`
- 变更（仅横向扩缩自有文件，共享模块零改动）：
  - `HorizontalScaleModal/index.tsx`：移除 `WorkloadContainerSelector`，内联仅含「工作负载」的 `Form/Select`；删除容器传参与 `SELECT_CONTAINER` 派发。
  - `horizontalScale/machine.ts`：删除 context 的 `container`/`containerNames`、`SELECT_CONTAINER` 事件、`ContainerOption` 导入；`loadingWorkloads.onDone` 改为 `buildRows(bundle.workloads)`。
  - `horizontalScale/rows.ts`：`buildRows` 去掉 `container` 形参与 `podContainers` 过滤，每集群一行。
  - `horizontalScale/__tests__/rows.test.ts`：改写容器过滤用例为「返回全部集群」。
- 行为变化：横向扩缩不再按容器过滤，展示 Group 下全部集群工作负载（副本数为 workload 级属性，展示全量更契合语义）。
- 未改动 `shared/WorkloadContainerSelector` 与 `shared/loader.ts` → 纵向扩缩 / 重启无影响。
- 验证：`yarn lint-type` 通过（0 error）；`yarn test src/pages/Workloads/operations` 27 例全绿（horizontal 6 / restart 8 / vertical 10 / batch 3）；`yarn lint` 无新增问题（4 error + 6 warning 全在未改动文件的既有基线中，horizontalScale/machine.ts 已不再触发 max-lines）。
- 变更未提交，留待用户决定。

## 集群下拉「集群名称」前置集群类型 icon

- 需求：`ClusterDropdown` 表格「集群名称」列在集群名前渲染 `clusterConnector` 对应类型 icon（icon 已置于 `src/assets/illustrations/`）。
- 变更（仅 breadcrumb 自有文件）：
  - `clusterConnectorIcons.ts`（新增）：import 9 个 illustration svg（作为 URL），维护 `clusterConnector`（大写归一）→ icon URL 映射，导出 `getClusterConnectorIcon`。映射：EKS-CCE→eks-cce、EKS-ECI→eks-eci、EKS-FED→eks-fed、SCI→eks-sci、BCE-CCE→bce、AWS-EKS→aws、GCP-GKE→gcp、K8S→k8s、K0S→k0s。
  - `ClusterDropdown.styles.ts`：新增 `ClusterNameCell`（flex + `spacing.xs` 间距，`.cluster-connector-icon` 16×16 不收缩）。
  - `ClusterDropdown.tsx`：`clusterName` 列 render 改为 `<ClusterNameCell>` 内 icon（命中才渲染，多彩插画走 `<img>`）+ 集群名。
- 备注：未知类型不渲染 icon（仅显示名称）；SCI 因无独立 `sci.svg`，映射到 `eks-sci.svg`（最近匹配），如有专属图标可再调整。
- 验证：`yarn lint-type` 通过（0 error）；`yarn lint` 三个改动文件 0 error/0 warning（仓库其余 4 error+6 warning 均在未改动文件的既有基线中）。
- 变更未提交，留待用户决定。


## 新增交互规范 + 相对时间 Hover 绝对时间补全

- 新建 owner doc `docs/design/interaction-guidelines.md`（收敛通用交互约定），并在 `docs/design/README.md`、`docs/index.md` 挂索引。当前收录两条：文本截断须配 Tooltip；相对时间须支持 Hover 显示绝对时间（绝对时间用 `dayjs` 格式化，默认 `YYYY-MM-DD HH:mm:ss`）。
- 依据第二条规范排查 Workload 相对时间展示，补全 3 处缺失的 Hover 绝对时间：
  - `duration.ts`：新增纯函数 `formatAbsoluteTime`（`YYYY-MM-DD HH:mm:ss`，缺失/非法返回 `-`）。
  - `podCells.tsx`：`renderAge` 用 Tooltip 包裹存活时长展示绝对创建时间；`renderRestarts` 的 `lastStartedAt` 改走 `formatAbsoluteTime`。
  - `BasicInfoCard.tsx` / `ContainerDetail.tsx`：「存活时间」加 Tooltip（有 `creationTimestamp` 时）。
- 验证：`yarn lint-type` 通过（0 error）；`yarn test` 覆盖改动的 `duration.test.ts` 等 Workloads 用例全绿。全量 `yarn test` 有 6 例失败，均在未改动模块（navigation context accountId 字符串/数字、GPU usageMerge、菜单路径、网络依赖 401 套件），与本次改动无关（相关文件不 import 被改代码）。
- 变更未提交，留待用户决定。

## 集群下拉「集群名称」前置集群类型 icon

- 需求：`ClusterDropdown` 表格「集群名称」列在集群名前渲染 `clusterConnector` 对应类型 icon（icon 已置于 `src/assets/illustrations/`）。
- 变更（仅 breadcrumb 自有文件）：
  - `clusterConnectorIcons.ts`（新增）：import 9 个 illustration svg（作为 URL），维护 `clusterConnector`（大写归一）→ icon URL 映射，导出 `getClusterConnectorIcon`。映射：EKS-CCE→eks-cce、EKS-ECI→eks-eci、EKS-FED→eks-fed、SCI→eks-sci、BCE-CCE→bce、AWS-EKS→aws、GCP-GKE→gcp、K8S→k8s、K0S→k0s。
  - `ClusterDropdown.styles.ts`：新增 `ClusterNameCell`（flex + `spacing.xs` 间距，`.cluster-connector-icon` 16×16 不收缩）。
  - `ClusterDropdown.tsx`：`clusterName` 列 render 改为 `<ClusterNameCell>` 内 icon（命中才渲染，多彩插画走 `<img>`）+ 集群名。
- 备注：未知类型不渲染 icon（仅显示名称）；SCI 因无独立 `sci.svg`，映射到 `eks-sci.svg`（最近匹配），如有专属图标可再调整。
- 验证：`yarn lint-type` 通过（0 error）；`yarn lint` 三个改动文件 0 error/0 warning（仓库其余 4 error+6 warning 均在未改动文件的既有基线中）。
- 变更未提交，留待用户决定。

