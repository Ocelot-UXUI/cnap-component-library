# 2026-08-05

## 横向扩缩去除容器选择器 + 内联工作负载选择

- Plan：`docs/plans/2026-08-05-horizontal-scale-remove-container-selector-plan.md`
- 变更（仅横向扩缩自有文件，共享模块零改动）：
  - `HorizontalScaleModal/index.tsx`：移除 `WorkloadContainerSelector`，内联仅含「工作负载」的 `Form/Select`；删除容器传参与 `SELECT_CONTAINER` 派发。
  - `horizontalScale/machine.ts`：删除 context 的 `container`/`containerNames`、`SELECT_CONTAINER` 事件、`ContainerOption` 导入；`loadingWorkloads.onDone` 改为 `buildRows(bundle.workloads)`。
  - `horizontalScale/rows.ts`：`buildRows` 去掉 `container` 形参与 `podContainers` 过滤，每集群一行。
  - `horizontalScale/__tests__/rows.test.ts`：改写容器过滤用例为「返回全部集群」。
- 行为变化：横向扩缩不再按容器过滤，展示 Group 下全部集群工作负载（副本数为 workload 级属性，展示全量更契合语义）。
- 未改动 `shared/WorkloadContainerSelector` 与 `shared/loader.ts` → 纵向扩缩 / 重启无影响。
- 验证：`yarn lint-type` 通过（0 error）；`yarn test src/pages/Workloads/operations` 27 例全绿（horizontal 6 / restart 8 / vertical 10 / batch 3）；`yarn lint` 无新增问题（4 error + 6 warning 全在未改动文件的既有基线中，horizontalScale/machine.ts 已不再触发 max-lines）。
- 变更未提交，留待用户决定。

## 新增交互规范 + 相对时间 Hover 绝对时间补全

- 新建 owner doc `docs/design/interaction-guidelines.md`（收敛通用交互约定），并在 `docs/design/README.md`、`docs/index.md` 挂索引。当前收录两条：文本截断须配 Tooltip；相对时间须支持 Hover 显示绝对时间（绝对时间用 `dayjs` 格式化，默认 `YYYY-MM-DD HH:mm:ss`）。
- 依据第二条规范排查 Workload 相对时间展示，补全 3 处缺失的 Hover 绝对时间：
  - `duration.ts`：新增纯函数 `formatAbsoluteTime`（`YYYY-MM-DD HH:mm:ss`，缺失/非法返回 `-`）。
  - `podCells.tsx`：`renderAge` 用 Tooltip 包裹存活时长展示绝对创建时间；`renderRestarts` 的 `lastStartedAt` 改走 `formatAbsoluteTime`。
  - `BasicInfoCard.tsx` / `ContainerDetail.tsx`：「存活时间」加 Tooltip（有 `creationTimestamp` 时）。
- 验证：`yarn lint-type` 通过（0 error）；`yarn test` 覆盖改动的 `duration.test.ts` 等 Workloads 用例全绿。全量 `yarn test` 有 6 例失败，均在未改动模块（navigation context accountId 字符串/数字、GPU usageMerge、菜单路径、网络依赖 401 套件），与本次改动无关（相关文件不 import 被改代码）。
- 变更未提交，留待用户决定。
