# 2026-07-08

- 开始实现面包屑维度选择器重构 plan (`docs/plans/2026-07-08-cnap2-breadcrumb-context-selectors-refactor.md`)：
  - Phase 1: 安装 `@xstate/react`；重构 `navigationContextMachine` 为带 `entry invoke` 的 machine，启动时自动加载 candidates；新增 `navigationOptionGroupMachine` 子 actor，负责 account/application/environment option group 的异步加载；父 machine 在 `selectAccount`/`selectApplication` 时通知子 actor；localStorage 持久化下沉到 machine actions；拆分 `navigationContextSnapshot.ts`、`navigationContextReducer.ts`、`navigationOptionGroupTypes.ts` 控制文件行数。
  - Phase 2: 新增 `navigationActor.ts`（模块级 `createActor().start()`）；`NavigationContext.tsx` 退化为极薄 wrapper（`NavigationProvider` 只渲染 children）；移除 `useNavigationContext`，消费者改用 `useNavigationSnapshot` / `useNavigationOptionGroups` + `navigationActions`；迁移 5 个消费点；`contextCapabilities.ts` 移除 `cachedCandidates` 缓存，改为 `navigationActor.getSnapshot()`。
  - Phase 3: 删除 `useBreadcrumbSelectorOptionGroups`、`useBreadcrumbSelectorSearch`、`breadcrumbContextFilter`、`breadcrumbContextTabs`；option group 数据由 XState 子 actor 管理。
  - Phase 4: 替换 AntD `Select` 为 `Dropdown`（trigger=click）；新增 `DimensionSelector` 共享触发器 + 各维度独立下拉面板（`AccountDropdown`/`ApplicationDropdown`/`EnvironmentDropdown`）；移除 `BreadcrumbSelectorConfig` 万能配置；触发器文本溢出改用 CSS `text-overflow: ellipsis`（末尾省略）；移除 `getMiddleEllipsisText`；选中态高亮跨维度一致；拆分 `dimensionPanel.styles.ts` 和 `dimensionDropdown.styles.ts` 控制行数。
  - `yarn lint-type`、`yarn test`（10 文件 / 69 测试）、`yarn build` 通过；`yarn lint` 剩余 8 个错误均为预存问题，本次变更未引入新 lint 错误。
- 落地 CNAP 2.0 Design Tokens 体系：新增 `src/constants/colors/palette.ts`（gray/primary/brand/navigation/success/warning/error 各 10 阶原子色板）、`semantic.ts`（bg/border/text/icon/button/state 语义 token，含 state.component 收敛边框/下拉/focus 中性色）、`navigation.ts`（sidebar 一二级独立 token）；新增 `radius.ts` / `spacing.ts` / `shadow.ts` / `typography.ts` 四个尺度 token 文件。全部 camelCase，`@/constants/colors` 聚合导出。
- 新增 `cnap2` antd 主题 preset 到 `src/constants/themes/presets.ts`：`colorPrimary=#41D08D`（品牌绿），Button 主要按钮走深色 `#1C202B` + 薄荷绿 `#A7F3CF` 文字；关键在于通过 `controlItemBgHover=#F2F2F2` / `controlOutline=rgba(24,24,24,0.06)` 等 alias token + Input/Select/DatePicker/TreeSelect/Cascader/Mentions 组件级 `hoverBorderColor=#BFBFBF`/`activeBorderColor=#181818` 显式钉死中性交互面，避免品牌绿污染 antd primary 派生链路。同步 `themeOptions` 增加 CNAP 2.0 入口。
- 旧 `src/constants/colors/base.ts` 加 `@deprecated` 注释，保留兼容存量代码，新代码禁止引用。
- 将默认主题从 `blue` 切换为 `cnap2`（`src/contexts/ThemeContext.tsx`）；已缓存 `THEME_KEY=blue` 的老用户需清 localStorage 才能看到新主题。
- 创建 `docs/design/design-tokens.md` 强制规范文档，覆盖 token 目录结构、引用优先级、品牌色分布决策矩阵、semantic 快查表、反模式、污染防护原理、AI Agent 使用指引；目标是非前端人员用 agent 写前端代码时能保持组件规范一致性。
- 将 design-tokens 规范广播到各主流 AI 编辑器：`AGENTS.md` 追加 "Design Tokens (强制)" 章节与 Read-First 引用；新增根目录 `CLAUDE.md`、`.cursor/rules/index.mdc`、`.cursor/rules/design-tokens.mdc`、`.github/copilot-instructions.md`、`.comate/rules/design-tokens.mdr` 并更新 `.comate/rules/index.mdr` 目录；`.claude/claude.md` 扩展为完整红线提要。覆盖 Codex（AGENTS.md）、Claude Code（CLAUDE.md）、Cursor（.cursor/rules）、Copilot（.github/copilot-instructions.md）、Comate（.comate/rules）。
- 验证：`yarn lint-type` 通过、`yarn lint` 仅剩既有无关问题。
- 修复 CR P2 反馈：
  - `src/routers/AppLayout/index.tsx` 拆出 `AppLayoutBody` 子组件，让 `theme.useToken()` 在 `<ConfigProvider theme={themeConfig}>` 内部执行，切到 cnap2 默认主题后 `WorkspaceContentLayout` 的背景 / 边框能正确读到 cnap2 token（此前读的是外层无主题 provider 的 antd 默认值）。
  - 将 `themePresets.cnap2` 与 `themeOptions` 里的 hex / rgba / 数字硬编码全部替换为 `palette` / `semantic` / `sidebar` / `radius` / `shadow` / `typography` 引用；仅 focus outline / bg mask 的 alpha 派生值保留为 preset 内 `FOCUS_OUTLINE` / `ERROR_FOCUS_OUTLINE` / `BG_MASK` 常量，就地组合，避免污染 palette / semantic 命名空间。
  - `src/design/Layouts/PageLayout.tsx` 从 `colors['gray-2']` 迁移到 `palette.gray[2]`；`SectionCard.tsx` 从 `colors.white` + 手写三层 rgba 阴影迁移到 `palette.gray[0]` + `shadow.s`（视觉与 Figma 卡片轻投影规范对齐）。
- 新增 `scripts/verify-design-tokens.ts` 机器约束（接入 `yarn verify:design-tokens`）：opt-in 模式，任何 import 了 `@/constants/{colors,radius,spacing,shadow,typography}` 的文件禁止出现 hex / rgb / rgba 字面量；全项目禁止 import from `@/constants/colors/base` 或引入已 `@deprecated` 的 `colors` / `myColors`。首轮清理后当前 baseline = 0 违规。
- 验证：`yarn lint-type` / `yarn verify:component-styles` / `yarn verify:design-tokens` / `yarn verify:router-paths` / `yarn build` 均通过；`yarn lint` 仅剩既有 9 处与本次改动无关的 max-lines / no-explicit-any / no-unused-vars 问题。
