# 2026-07-01

- 创建 Agent Navigation Context Capabilities 执行计划草案，覆盖结构化 navigate tool result、Agent loop 结果回传、XState 上下文状态机、Agent capability registry、prompt 硬事实边界和验证门控；当前状态 proposed，需独立 draft review 后再实现。
- 实施 Agent Navigation Context Capabilities：新增 `AgentToolResult` 结构化结果、改造 `navigate` 与 `agentLoop` 回传、引入 XState 管理账号/应用/环境上下文、建立首批 Agent capability registry，并从 registry 派生 AI tool schema。
- 验证：`yarn lint-type` 通过；`yarn test` 通过（9 files / 62 tests）；`yarn build` 通过；`yarn lint` 已运行但仍失败于既有无关 lint 债（LoadingProgress any、navigation registry/max-lines、RuntimeConfig 组件 max-lines、date unused catch、i18n any）。
- 创建独立 Mock Navigation Context API 计划，用于增加人工验证所需的用户到账户、账户到应用、应用到环境三类 mock-backed API 边界；状态 proposed，等待 draft review 后实现。
- 实施 Mock Navigation Context API：新增 `src/api/navigationContext` 三类异步 mock-backed API，集中账号/应用/环境 mock 数据源，`NavigationContext` 通过 API-like adapter hydrate 候选数据，Agent context 能力复用同一候选来源；计划等待 closure audit 后关闭。
- 收紧 Mock 数据边界：`src/api/navigationContext/mockData.ts` 不再向业务层导出，导航上下文存储、状态机、Provider 与 Agent context 能力只能通过 API 函数或 API-backed candidate loader 获取候选数据。
- 验证：`yarn lint-type` 通过；`yarn test` 通过（10 files / 67 tests）；`yarn build` 通过；`yarn lint` 已运行但仍失败于既有无关 lint 债（LoadingProgress any、navigation registry/max-lines、RuntimeConfig 组件 max-lines、date unused catch、i18n any）。
- 应用管理页移除页面内 `mockApplications`，改为基于当前资源账户调用 `getApplicationsByAccount` 获取应用列表，只使用接口返回的 `id` 和 `name` 渲染最小可用列表/卡片视图。
- 应用详情五个 tab 路由（概览、部署管理、运行配置、启动配置、设置）声明账号和应用上下文需求，全局 header 显示应用维度选择器；切换 header 应用维度时，`ApplicationLayout` 保持当前 tab 并替换 URL 中的 `appId`。
- 关闭 Mock Navigation Context API 计划：该计划定位为人工验证辅助的小修改，已满足三类 mock-backed 接口和最小页面验证需求。
- 重新打开 Agent Navigation Context Capabilities 主计划的修补阶段：补充 Phase 6，处理路由参数到 XState 导航上下文状态机的同步缺口，避免 Agent 导航后 header 选择器状态与目标路由不一致。
- 完成 Agent Navigation Context Capabilities Phase 6：新增受控 `syncRouteContext` 入口，路由中的应用/环境参数会同步到 XState 导航上下文并推导上级账号/应用；应用详情布局优先吸收 Agent 导航带来的 `appId`，同时保留 header 手动切应用时替换当前 tab 路由的行为。
- 验证：`yarn lint-type` 通过；聚焦测试 `navigationContextMachine`、`derive`、`navigationTool` 通过；`yarn build` 通过。
- 关闭 Agent Navigation Context Capabilities 主计划：人工审查通过，结构化工具结果、XState 上下文、Agent capability registry、Mock API 验证辅助、应用详情维度联动和路由参数同步修补均已完成。
- 归档 CNAP 2026 Figma 源材料链接：新增视觉设计和交互设计索引，覆盖导航、导航优化和工作负载模块；当前为原始输入，尚未合成为实现级需求。
- 分析 Figma 节点“布局规则”，未进行 figma2code 编码实现；新增 `docs/design/application-layout-guidelines.md`，沉淀 CNAP 应用外壳、顶部导航、一级/二级侧边导航、内容区间距和收敛规则。
- 继续分析导航优化 Figma 节点，未进行编码实现；补充源材料索引，并扩展导航 UI 规范，覆盖二级导航展开/收起、页面下一级分类、小屏更多入口、更多弹层和从更多选择隐藏工作区后的布局行为。
- 根据人工补充信息修正导航布局规范：线上/沙盒使用云上百度统一 Header，本地 Header 仅作占位；一级导航业务图标与底部辅助图标分区，更多项支持激活图标替换与高度自适应收敛；二级导航展开宽度 200px、收起宽度 60px；主内容区始终保持左右 32px 间距。
- 补充一级导航更多入口设计意图：更多仅作为 overflow trigger，没有 active 态；active 态始终属于业务入口，隐藏业务入口被激活后需提升到一级导航可见区域，以同时保证当前位置可见和导航容量稳定。
- 补充导航布局实现原则：业务图标收敛以业务区和辅助区间距小于 52px 为触发条件，替换结果在会话内通过 LocalStorage 保持；二级导航折叠状态作为会话内独立布局状态保留；页面级分类 tabs 必须实现为子路由，保证完整 URL 可直达。
- 明确导航配置模型保持轻量：仅区分一级业务入口和二级业务入口；页面级分类 tabs 独立于一级/二级导航；底部辅助入口和更多入口作为固定 UI 控件在组件内实现。
- 创建下一步实现需求 `docs/requirements/cnap2-application-layout-navigation.md`，将应用外壳、一级导航溢出、更多弹层、二级导航折叠、内容区间距和页面分类子路由整理为可执行需求。
- 修正一级导航溢出判断原则：不通过累计业务图标配置高度判断，而是测量业务图标容器、底部辅助入口容器和一级导航容器，基于两块区域之间的实际距离是否小于 52px 触发收敛。
- 关闭应用布局导航需求的 open questions：本地顶部上下文选择器需要对齐 Figma Header 占位视觉，二级侧边导航折叠入口纳入首个实现切片。
