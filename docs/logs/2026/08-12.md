# 2026-08-12

## Playground 增补 Drawer / Modal 尺寸档示例

Source: 需求（会话内）。

- `src/pages/ComponentPlayground/sections/Overlays.tsx`：DrawerSection / ModalSection 各提供 S / M / L 三个触发按钮，通过 size state + open state 打开对应档位（Drawer 600/800/980，Modal 600/800/1024），替代原先单档 width=480 / 默认宽示例。

验证：`yarn lint-type` 通过。

## Playground 组件示例 URL 同步

Source: 需求（会话内）。

- `src/pages/ComponentPlayground/index.tsx`：activeKey 由 useState 改为完全从 `?section=<key>` 派生（`useSearchParams`），点击左侧列表用 `replace` 写回 URL。分享 / 重开 URL 可直达对应组件，浏览器前进 / 后退也能同步；非法参数回退到第一个组件。

验证：`yarn lint-type` 通过。

## 组件视觉验收修复（第一轮）

Source: 设计验收文档《cnap2.0 组件 设计验收》+ 会话内设计侧数值澄清。
Plan: `docs/plans/2026-08-12-component-visual-acceptance-fixes-plan.md`

主题 token（`src/constants/themes/presets.ts`）:

- Button 去投影（primary/default/danger）、补 default 禁用背景与禁用边框
- Switch 默认档轨道宽 32 / 滑块 16
- Input 字号 12/14、圆角统一 8px（仅 Input）
- InputNumber 小尺寸字号 12
- Table `rowHoverBg` 由 warning-01（橙）改为 gray-02，橙色归还业务侧「核验态」行
- Pagination 激活项去灰底
- Tag 去描边、Tooltip 投影收敛为 `shadow.xs`

全局样式（`src/styles/global-table.ts`、`src/styles/reset.css`）:

- Checkbox 选中 / 半选 / hover / 禁用统一走 `semantic.state.brand.*`，删除与 preset 冲突的深色主按钮底覆盖（这是「绿色不是一种」的根因）
- Radio 已选禁用态：默认态 + 60% 透明度、内圆 6px（受控局部值 + 注释，未新增 token）
- Tag 无描边 / 关闭图标色 / 置灰态；Pagination 省略号色、激活项不加粗、条数选择器下拉圆角 8px / 选项 6px
- Switch small 档轨道宽 24 / 滑块 10（antd small 由 class 固定，token 改不到）
- `.ant-5-btn-sm` 硬编码 12px 改引用 `typography.body.small`
- 删除 `.ant-5-message` 右上角定位与右滑入动画，恢复 antd 顶部居中（这是 playground 里 Message 在右上角弹出的根因）

组件（`src/design/`、`src/components/`、`src/index.tsx`）:

- Drawer 新增 `size` s/m/l → 600/800/980，默认 m
- Modal 由透传改为增强组件：`size` s/m/l → 宽度 600/800/1024 + 内容区 300~600 / 480~600 / 560~800，保留 confirm/useModal 等静态方法
- Select 多选态显式给 `suffixIcon`，避开 antd 在可搜索下拉展开时把箭头替换成搜索图标的分支
- LogSearchInput：prefix 与上下切换图标改用 `@/assets/icons` 的 Search / ArrowUp / ArrowDown，匹配数字字体显式走 PingFang SC

Owner doc 同步：`docs/design/design-tokens.md`（品牌色分布表补 Checkbox / Table hover / Pagination；新增「组件尺寸档」小节）。

范围外（设计侧明确本轮不做）：面包屑可下拉、Empty 插画与尺寸、InputNumber hover 边框、Input 出错态 hover 气泡、Tab / 导航 / 折叠面板 / 置顶球 / 信息分栏 / 底部操作 bar。

验证：

- `yarn lint-type` 通过
- `yarn lint` 无新增问题（2 个 error 位于 `src/utils/i18n/index.ts`、6 个 max-lines warning 均为改动前既有）
- `yarn test` 37 个测试文件全部 collect 失败（`TypeError: Cannot read properties of undefined (reading 'config')`）；在 stash 掉本次改动的干净基线上复现同样失败，属既有问题，非本次引入。**未通过，不作为通过基线记录。**

## Design Empty 插图扩展

Source: 当前用户需求。
Plan: `docs/plans/2026-08-12-design-empty-illustration-plan.md`

- `src/design/Empty` 由 antd 透传升级为 drop-in 增强组件，新增五种 `imageType`：`empty-table`、`empty`、`no-auth`、`no-data`、`no-target`。
- 新增 `size`：S 125×110 / M 175×154 / L 248×218，默认 M；仅在 `imageType` 有效时生效，未传或运行时无效时完整回退 antd 原生 Empty。
- 保留 antd 原生参数和 `PRESENTED_IMAGE_DEFAULT` / `PRESENTED_IMAGE_SIMPLE` 静态成员；统一出口新增 `EmptyProps`、`EmptyImageType`、`EmptySize`。
- Playground 增加 CNAP 插图和尺寸示例；组件契约与设计尺寸文档已同步。
- Empty Playground 使用两个下拉框分别选择 `imageType` 与 `size`，只展示当前组合，默认 `empty + M`。
- 新增 18 项 Empty 测试，覆盖资源映射、尺寸、默认值、原生回退、原型链键名、样式语义、静态成员及 Props 透传。

验证：

- `yarn lint-type` 通过
- `yarn test src/design/Empty/Empty.test.tsx` 通过（18 tests）
- `yarn test` 通过（38 files / 244 tests）
- Empty 相关文件 ESLint 与 dprint 定向检查通过
- `yarn lint` 未通过：`src/utils/i18n/index.ts` 两处既有 `no-explicit-any` error，另有 7 个既有 max-lines warning；本次相关文件无问题
