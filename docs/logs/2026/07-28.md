# 2026-07-28

## refactor：批量伸缩/重启弹窗表格接入 BaseTable

将 workload 其余操作弹窗中的集群表格统一改用共享 `@/design/Table` 的 `BaseTable`：`verticalScale`、`restart`、`horizontalScale` 三个 `ClusterTable` 移除直接引用的 antd `Table` 与 `size="small"`，改用 BaseTable（默认 small），各自保留列配置、`scroll`、`locale`、Checkbox/Input 渲染等行为。至此 workload 操作弹窗与 Pod 详情容器表格样式全部收敛到 BaseTable。

### 验证

- 三个 ClusterTable 目标文件 ESLint 通过（exit 0）。
- `yarn lint-type` 通过（exit 0）。
- 相关单测为纯逻辑（rows.test.ts 等），不覆盖表格渲染，无需改动。

## style：Pod 表格 CPU/内存列加小图标

Pod 表格 CPU、内存列在百分比前增加对应的 CPU/内存小图标（16px，复用 `@/assets/pod-resource-usage`）。渲染逻辑在 `podUsageCells.tsx` 的 `renderUsageCell` 增加 `icon` 入参，新增 `podUsageCells.style.ts` 提供 `UsageCell`（inline-flex + 图标文字间距）与 `UsageCellIcon`；hover 气泡与百分比展示不变。

### 验证

- 目标文件（`podUsageCells.tsx` / `podUsageCells.style.ts`）ESLint 通过（exit 0）；`yarn lint-type` 通过（exit 0）。

## feat：资源用量 hover 气泡（CPU/内存）

依据 Figma Frame 618:28869（f2c）新增资源用量 hover 气泡，供 Pod 详情容器资源用量与 Pod 表格 CPU/内存列复用。新增共享组件 `PodDetailDrawer/ResourceUsageTooltip`（白底 antd `Tooltip` + 自定义内容：标题、进度条 + `usage/limit` 百分比、Limit/usage/request 三行图例），沿用 `resourceUsage.ts` 纯逻辑计算百分比与高负载态（≥80% 转 error 红）。图例圆点配色对齐 Figma：Limit=error 红、usage=`navigation[3]`、request=`border.divider`。`ResourceUsageView` 的 CPU/内存指标块与 Pod 表格单元格分别包裹该 Tooltip。为控制 `podCells.tsx` 行数，将 CPU/内存单元格渲染拆到新文件 `podUsageCells.tsx`，`podColumns` 从其导入。GPU 气泡按指令暂缓。

Skill: figma2code

### 验证

- 目标文件（`ResourceUsageTooltip.tsx/.style.ts`、`ResourceUsageView.tsx`、`podCells.tsx`、`podUsageCells.tsx`、`podColumns.tsx`）ESLint 通过（exit 0）。
- `yarn lint-type` 通过（exit 0）。
- PodContentArea 全部单测 36 项通过。
- antd 6 Tooltip 语义样式键为 `root/container/arrow`（无 `body`），内容内边距经 `styles.container` 设置。

## style：容器资源用量按 Figma 展示（CPU/内存/GPU）

依据 Figma Frame 1912058193（f2c）重做容器详情的资源用量区：新增 `ResourceUsageView` 组件与同目录 `ResourceUsageView.style.ts`，替换原 `ContainerDetail` 内联的 `UsageRow` 与 GPU 文本行。CPU/内存改为竖排三段：数值行 `usage/request/limit`（图标 + 文本）、进度条 + `usage/limit` 百分比（高负载 ≥80% 时数值与进度条转 error 红）、底部指标名；分隔用 48px 竖线，`资源用量` 前缀标签。GPU 展示为 Figma 导出的品牌图标 + 型号 + `×数量`，图标从 Figma 整体导出复用于所有品牌（`src/assets/pod-resource-usage/{cpu,memory,gpu}.png`，源 image_1/4/6）。展示口径复用既有 `resourceUsage.ts` 纯逻辑，未改数据契约。新增组件文件因与既有 `resourceUsage.ts` 仅大小写不同触发 TS1149，故命名为 `ResourceUsageView`。

Skill: figma2code

### 设计取舍

- Figma GPU 卡片用 NVIDIA 绿 `#76B900`，无对应 design token，且 token 规范禁止 hex 字面量/品牌绿用于卡片边框。按用户决定：绿色由 Figma 导出的 GPU 图标承载，卡片底/边框/文字改用中性 token（`bg.page` / `border.divider` / `text.secondary`），未硬编码 `#76B900`。如需完整绿色卡片，应在 palette/semantic 新增 GPU 品牌色 token 或整块导出为图片。

### 验证

- 目标文件（`ResourceUsageView.tsx` / `ResourceUsageView.style.ts` / `ContainerDetail.tsx`）ESLint 通过（exit 0）。
- `yarn lint-type` 通过（exit 0）。
- PodDetailDrawer 相关单测 8 项全部通过。
- 全量 `yarn lint` 仍为仓库既有 5 errors / 3 warnings，均不在本次目标文件；全量 `yarn test` 仍受既有导航断言与 jsdom 缺少 ResizeObserver 的 suite 失败影响，与本次改动无关。

## refactor：抽取共享基础表格组件 BaseTable

将 Pod 详情抽屉容器区（端口/挂载/环境变量）中的轻量表格样式与批量操作弹窗表格（PodPreviewTable、ClusterParamsTable）统一为 `src/design/Table/BaseTable`。BaseTable 仅统一样式（白色表头/表体、48px 行高、表头三级色中等字重、去表头竖分隔线、保留浅灰横向分割线、首列贴左），行为与 antd Table 一致，默认 `size="small"` 并合并调用方传入的 className。`detailTables.tsx` 移除本地 `detailTableStyle` 改用 BaseTable；批量弹窗两张表也改用 BaseTable，各自保留列配置、`scroll`、`rowKey` 等行为。批量重启弹窗因复用同一批量表格组件一并生效。

### 验证

- 本次目标文件（BaseTable/detailTables/PodPreviewTable/ClusterParamsTable）ESLint 通过（exit 0）。
- `yarn lint-type` 通过（exit 0）。
- 相关模块单测均为纯逻辑测试（filterParams/usageMerge/containerOrder/resourceUsage 等），不覆盖表格渲染，无需改动。
- 全量 `yarn lint` 仍为仓库既有 4 errors / 3 warnings，均不在本次目标文件。

## style：批量删除/重建 Pod 弹窗样式

依据 Figma Frame 1912057558 调整 `DeleteModalBase` 弹窗内容区样式：新增同目录 `DeleteModalBase.style.ts`（局部私有样式，不改共享的 `batch/batchModal.style.ts`，避免影响批量重启弹窗）；section 标题（待操作 Pod、集群与参数配置）前加 Brand-04 绿色强调条，警告提示由 antd `Alert`（带图标）替换为无图标的暖色提示条（`warning.light` 底 + 次要文字 + 8px 圆角），并统一 section 间 24px 间距。仅改样式，未改动 props、数据、提交逻辑；底部按钮与右上关闭按钮不在改动范围内。

### 验证

- 本次目标文件（`DeleteModalBase.tsx` + `DeleteModalBase.style.ts`）ESLint 通过（exit 0）。
- `yarn lint-type` 通过（exit 0）。
- 全量 `yarn lint` 仍为仓库既有 4 errors / 3 warnings，均不在本次目标文件。

## feat：Pod 详细模式展示双行端口

Pod 表格端口列根据视图模式控制可见数量：详细模式展示前两个端口为两行，精简模式保持展示首个端口；存在更多端口时，在末行显示剩余数量 `+n`，鼠标悬停端口内容后通过 Tooltip 逐行展示所有未显示端口。

### 验证

- Pod 端口目标文件 ESLint 通过（exit 0）。
- `yarn lint-type` 通过（exit 0）。
- `yarn build` 通过（exit 0）；生成文件尾换行副作用已清理。
- `git diff --check` 通过（exit 0）。
- `yarn lint` 仍为仓库既有 4 errors / 3 warnings，本次目标文件无错误或告警。
- `yarn test`：146 passed / 1 failed / 16 skipped；失败仍为既有导航路径断言、接口 401 和 jsdom 缺少 ResizeObserver，与本次端口展示改动无关；PodContentArea 现有测试均通过。

## style：Pod 基本信息卡片折叠样式

依据 Figma 调整 `BasicInfoCard`：展开态按钮跨在卡片底边中央，收起态移除卡片内容区域并仅保留 48×18px 展开按钮；同步 IP 字体与两行间距，未改动数据和业务逻辑。

### 验证

- 目标文件 ESLint、`yarn lint-type`、`git diff --check`、`yarn build` 通过。
- 全量 `yarn lint`、`yarn test`、组件样式和 Design Token 审计仍受上述仓库既有问题影响，本次目标文件无新增错误。

## style：Pod 容器 Tab 样式

依据 Figma Frame 1444 调整 Pod 详情抽屉的外层容器 Tab：增加浅灰底与 12px 圆角，统一 40px 标签栏，选中项使用白底，并为整体 Tab Content 增加白底与中性边框；样式仅作用于第一层 Tabs，未修改容器数据逻辑或详细信息、日志、终端、事件子 Tab 内容。

### 验证

- 目标文件 ESLint、`yarn lint-type`、`yarn build` 通过。
- PodDetailDrawer 相关测试 9 项全部通过。
- 全量 `yarn lint` 仍为仓库既有 4 errors / 3 warnings；全量 `yarn test` 仍受既有导航断言、接口 401 和 jsdom 缺少 ResizeObserver 影响。

## style：Pod 容器详情轻量表格样式

依据 Figma Frame 828 调整容器详情中的端口、挂载、环境变量表格：统一白色表头和表体、48px 行高、表头三级文字色及中等字重，移除表头纵向分隔线并保留浅灰横向分割线；未修改列配置、数据渲染与交互行为。

### 验证

- 目标文件 ESLint、`yarn lint-type`、`yarn build` 通过。
- PodDetailDrawer 相关测试 9 项全部通过。
- 全量 `yarn lint`、`yarn test`、`yarn format:check` 和 Design Token 审计仍受仓库既有问题影响，本次目标文件无新增错误。

## feat：Pod 容器端口地址复制

依据 Figma Frame 828，在容器端口表格新增操作列和逐行复制按钮，并在端口标题栏增加“复制全部 IP:PORT”入口；复制地址使用 Pod IP 与端口号拼接，批量内容按表格顺序逐行分隔，无 Pod IP 或无端口时禁用对应操作。

### 验证

- 目标文件 ESLint、dprint、`yarn lint-type`、`git diff --check`、`yarn build` 通过。
- 全量 `yarn lint` 仍为仓库既有 4 errors / 3 warnings，`yarn format:check` 仍受 `src/index.tsx` 既有导入顺序影响。
- `yarn test`：146 passed / 1 failed / 16 skipped；失败仍为既有导航路径断言、接口 401 和 jsdom 缺少 ResizeObserver，与本次复制功能无关。

## feat：同步 Pod Usage API 契约

重新抓取 Ku 运行时工作负载文档并校正 Usage 示例：ResourceQuota 统一为 quantity 字符串并支持结构化 GPU；新增批量和单 Pod Usage API，单 Pod 响应同时合并同类型的 `containers` 与 `initContainers` 用量；Pod 列表和详情在 Usage 失败时保留基础数据并将用量降级为不可用。同步补齐 PodOperation 字段，未改动 operation name 触发逻辑和容器日志 UI。

### 验证

- `yarn lint-type`、本次目标文件 ESLint、相关 16 项测试、`git diff --check`、`yarn build` 通过。
- Closure audit 指出的结构化 GPU 列表展示和重复 Usage 降级问题已修复并加入回归测试；非致命失败与 stale response 由 effect 的 catch/cancellation 控制流静态验证。
- 全量 `yarn lint` 仍为仓库既有 4 errors / 3 warnings，本次目标文件定向 ESLint 无问题。
- 全量 `yarn test`：148 passed / 1 failed / 16 skipped，并有 3 个既有 suite 因接口 401 与 jsdom 缺少 ResizeObserver 失败；唯一断言失败仍为导航侧栏期望 `/applications`、实际 `/workloads`，与本次 Usage 改动无关。
- 计划保持 partially completed，等待全量验证基线恢复后关闭。
