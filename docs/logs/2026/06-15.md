# 2026-06-15

## dprint + ESLint 工具链迁移

- 移除 reskript 工具链（@reskript/cli, @reskript/cli-lint, @reskript/config-lint, @reskript/settings）
- 引入 dprint 0.54.0 作为代码格式化工具（devDependency），创建 `dprint.json` 配置
- 重写 `.eslintrc.cjs`，使用纯 ESLint + typescript-eslint + eslint-plugin-react + eslint-plugin-react-hooks，仅保留代码质量规则
- 新增 scripts: `yarn format`, `yarn format:check`
- 更新 `yarn lint` 从 `skr lint` 改为 `eslint src --ext .ts,.tsx`
- 升级 yarn 从 v1.22.4 到 v4.13.0（通过 Volta）
- 添加 husky pre-commit hook，提交前自动运行 `yarn format`

验证结果:

- `yarn lint-type`: pass
- `yarn lint`: 运行正常，仅报告 8 个既有质量问题（complexity、no-explicit-any、max-lines、no-unused-vars），无格式错误
- `yarn test`: 34 tests pass
- `yarn build`: pass
- `yarn format`: 已通过 pre-commit hook 全量格式化 192 个文件并提交 (d30da32a)

Plan: `docs/plans/dprint-migration.md`
