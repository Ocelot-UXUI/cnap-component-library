# 2026-07-25

## 实现：集群选择器 + appEnvID 解析贯通（plan 2026-07-25-cnap2-cluster-selector）

执行 `docs/plans/2026-07-25-cnap2-cluster-selector-plan.md`，owner `v_wangkaiyuan02`（已校验 gitname 在 Owner 列表）。

### Phase 1 — appEnvID 解析 selector

- 已在既有未提交改动中落地并复核：`selectAppEnvID`（`src/contexts/navigationContextSnapshot.ts`）+ `useAppEnvID()`（`NavigationContext.tsx`）+ 单测 `__tests__/appEnvID.test.ts`（3 项通过）。
- 说明：`NavigationContextState.environmentId` 存储的即 `AppEnvironment.id`（= appEnvID），selector 按 `availableEnvironments` 校验有效性。

### Phase 2 — clusterId 维度契约扩展（公共契约变更）

- `navigationContextData.ts`：`NavigationContextState` 增 `clusterId?: string`；`normalizeNavigationContext` 环境无效时级联清除 clusterId；`isValidStoredContext` 校验 clusterId 为 string。
- `navigationContextSnapshot.ts`：`getInvalidContext` / `deriveRouteContext` 纳入 clusterId（clusterId 仅在 environmentId 有效时透传）。
- `src/navigation/types.ts`：`ContextKey` 增 `'clusterId'`。
- 新增 `__tests__/clusterContext.test.ts`（5 项）：归一化保留/丢弃、invalidContext。既有导航单测全绿（`toEqual` 忽略 undefined 属性，不破坏三维）。

### 评审门（进入 Phase 3 前）

- 用 General subagent 对 Phase 2 公共契约变更做**独立评审**：结论 PASS-WITH-NOTES（契约向后兼容、类型安全、localStorage 持久化无需额外接线）。
- 采纳 N1：Phase 3 在 `selectEnvironment` reducer 分支显式清除 clusterId（不依赖归一化）并加回归测试。
- B1（`derive.test.ts` 既有失败）triage 为既有回归，见 `docs/bugs/03-*`。

### Phase 3 — 状态机集群区域

- `navigationContextReducer.ts`：新增 `selectCluster`；`selectEnvironment` 显式清 clusterId；新增 `reloadClusters` 事件；`resolveSelection` 拆出 `resolveHierarchySelection` 降复杂度。
- `navigationContextMachine.ts`：`selectEnvironment` → `notifyOptionGroup{environmentChanged}`（新增通道，appEnvID = 归一化后的 environmentId）；`selectCluster` = applySelection；`reloadClusters` = 仅重发 environmentChanged；restore/sync/hydrate/restoreWorkspace/loading.onDone 一并补 environmentChanged。
- `navigationOptionGroupTypes.ts` / `navigationOptionGroupMachine.ts`：新增 `cluster` 并行区域 + `appEnvID` 上下文 + `loadCluster`（`getClusters({appEnvID})`）；`environmentChanged` 有效 appEnvID 时加载、否则 idle；`accountChanged`/`applicationChanged` 重置。
- `navigationActor.ts`：新增 `setClusterId` / `reloadClusters`。
- 新增 `__tests__/clusterNavigation.test.ts`（7 项）：selectCluster / 上层变更清 clusterId / optionGroup cluster 区域 loading·reset。

### Phase 4 — 面包屑集群选择器

- 新增 `ClusterDropdown.tsx` + `ClusterDropdown.styles.ts`（antd `Table`：集群名/类型/`{available}/{desired}`；本地搜索；单选/取消；Loading·Empty·Error+重试·未选环境占位；"绑定新集群"占位 footer）。
- 新增 `ClusterBreadcrumbSelector.tsx`（封装 DimensionSelector + ClusterDropdown，默认"全部集群"）。
- `BreadcrumbContextSelectors.tsx`：visibleDimensions 纳入第四维 cluster；`useBreadcrumbSelectorWidth` 因 itemCount 自动适配。
- **design tokens**：需求 hex 映射为 `semantic.state.component.selectHover`（行 hover）/`semantic.state.component.selectActive`（选中态浅绿），未照搬 hex。

### Phase 5 — 路由需求 + pilot 接线（关闭 Follow-up）

- `src/navigation/registry.ts`：`applications.workloads` 的 `contextRequirements` = `{accountId, applicationId, environmentId, clusterId}`。
- `WorkloadsHeader/index.tsx`：删除占位 `APP_ENV_ID=0`，改用 `useAppEnvID()`；`getWorkloadGroups({appEnvID, clusterId})` 随 clusterId 刷新并重置 groupId；`VerticalScaleModal` 接入真实 `appEnvID`/`clusterId`/`environmentName`，仅 appEnvID 有效时可开。

### 验证

- `yarn lint-type` 通过（exit 0）。
- `yarn build` 通过（exit 0，17.86s；仅既有 chunk 体积告警）。
- `yarn test` 全量 **105/106 通过**；唯一失败 `derive.test.ts > generates internal menu paths without basename` 为**既有回归**（`git stash` 干净基线即失败），已记 `docs/bugs/03-*`，与本改动无关。cluster-selector 新增/相关单测（appEnvID 3 + clusterContext 5 + clusterNavigation 7 + 既有导航机器 7）全部通过。
- `yarn lint`：仓库既有 lint 红（多为无关文件 max-lines/any/eqeqeq）。本次移除新引入的 eqeqeq/complexity，并将 `BreadcrumbContextSelectors.tsx` 拆分回 140 行内；`navigationContextMachine.ts`(143→161) 与 `navigationOptionGroupMachine.ts`(159→215) 在本次前**已超** 140 max-lines，属既有 lint 债，本 plan 验证门为 lint-type/test/build（未把 lint 作为通过基线）。

### 关闭

- 纵向扩缩 pilot 的 Follow-up（appEnvID/clusterId 接线部分）关闭；端到端后端联调按 Non-Goals 留待。
- 独立关闭审计（General subagent）通过（PASS-WITH-NOTES，无阻塞项）；plan `planned → completed`。
- 非阻塞注记（供后续跟踪）：`yarn test` 存在测试拆解期异步告警（`window`/`ResizeObserver` after teardown，源自 xstate actor 未 stop 的微任务），既有 `navigationContextMachine.test.ts` 与新 `clusterNavigation.test.ts` 均触发，不影响 105 项通过判定，属测试卫生债。

## 实现：标题栏工作负载操作弹窗补全 — 横向扩缩 + 重启（plan 2026-07-25-workload-operation-dialogs）

执行 `docs/plans/2026-07-25-workload-operation-dialogs-plan.md`，owner `v_wangkaiyuan02`（gitname 在 Owner 列表）。draft review：General subagent PASS-WITH-NOTES，2 Blocking（需求状态一致性 / 两弹窗合一 Decision）+ 5 建议已闭环后 `planned`。

### Phase 1 — 共享 loader 提取

- 新增 `operations/shared/loader.ts`（`loadGroups`/`loadWorkloads`/`aggregateContainerNames`/`WorkloadsBundle`）；`verticalScale/machine.ts` 改引 `../shared/loader`；删除 `verticalScale/loader.ts`（无 re-export 残留）。既有 `verticalScale/rows.test.ts` 10 项仍绿。

### Phase 2 — 横向扩缩弹窗

- 新增 `operations/horizontalScale/`：`rows.ts`（期望副本数默认取 `replicas`、正整数校验、选中行→targets，容器仅过滤不入 API）+ `machine.ts` + `selectors.ts` + `submit.ts` + `HorizontalScaleModal/`（`index.tsx`/`ClusterTable.tsx`/`*.style.ts`，Modal 宽 800、期望副本数 Input 非法标红、空态占位）。
- 单测 `__tests__/rows.test.ts` 6 项。

### Phase 3 — 重启弹窗

- 新增 `operations/restart/`：`rows.ts`（最大不可用百分比 1~100 校验、取消选中回滚原值、编辑未选中行自动选中、超时时间 5~3600 校验、容器入 API + `%` 拼接）+ `machine.ts`（含 `exitTimeout` 上下文 + `SET_TIMEOUT`）+ `selectors.ts` + `submit.ts` + `RestartModal/`（温馨提示暖色条走 `semantic.state.warning.light`、超时时间输入、Modal 宽 800）。
- 单测 `__tests__/rows.test.ts` 8 项。

### 标题栏接线

- `WorkloadsHeader`：抽出 `OperationModals.tsx`（`MODAL_CAPABILITIES` + `WorkloadOperationModals` 单组件按 capability 渲染 V/H/Restart），`handleActionClick` 用单一 `activeOp` 状态路由；此举同时将 `index.tsx` 收回 140 行内（消除新引入的 max-lines）。

### 验证

- `yarn lint-type` 通过（exit 0）。
- `yarn build` 通过（exit 0）。
- `yarn test`（Workloads/operations）**24/24 通过**（vertical 10 + horizontal 6 + restart 8）。
- `yarn lint`：新增文件（horizontalScale/restart/shared/OperationModals/header index）**无告警**；仓库剩余 9 项 max-lines/any/unused 均为既有 lint 债（`navigationContextMachine`、`navigationOptionGroupMachine`、`LoadingProgress`、`MountVolumeList`、`ResourceLimit`、`verticalScale/rows.ts`、`utils/date`、`utils/i18n`），非本次引入。

### 需求与关闭

- `horizontal-scale-dialog.md` / `app-restart-modal.md` 状态 → 已实现（2026-07-25）。
- 「Group 操作菜单带入」入口按 plan Non-Goals 留待 Pod 列表/分组行切片。
- 独立关闭审计（General subagent）PASS，无阻塞项；plan `planned → completed`。N1（重启弹窗 606px 高度）软化为参考标注（antd Modal 高度随内容自适应）。

## 实现：工作负载页 Pod 列表内容区（子需求 2，真实 API）（plan 2026-07-25-pod-list-content-area）

执行 `docs/plans/2026-07-25-pod-list-content-area-plan.md`，owner `v_wangkaiyuan02`。**范围变更**：用户 2026-07-25 指示本切片直接对接真实 API（非 mock），已更新 `pod-list-content-area.md` 与父需求 Out-of-Scope。draft review：General subagent PASS-WITH-NOTES（建议已闭环）→ planned。全部代码统一于 `src/pages/Workloads/PodContentArea/`。

### Phase 1 — 状态映射 + 纯逻辑

- `podStatus.ts`（状态→中文名/tone/是否正常，Init: 前缀归类，未命中回退 error/非正常）、`quickFilter.ts`（正常/异常/已屏蔽/全部 → status 值集合 + blocked + 计数）、`duration.ts`（存活 dayjs）。单测 16 项。

### Phase 2 — 数据编排（真实 API）

- `types.ts`（PodFilterState/GroupQuery）、`filterParams.ts`（筛选态→getPods 参数，status 逗号拼接）、`selection.ts`（跨组累计）、`usePodGroups.ts`（getWorkloadGroups + getRuntimeSummary(全局) + getOperations）、`useGroupPods.ts`（每组 getPods，筛选变更重置页码，展开态懒加载，防止 stale-page 抓取）。单测 filterParams 2 + selection 3。

### Phase 3 — 骨架 + 标题栏 + 筛选区 + 快捷筛选联动

- `PodContentArea/index.tsx` 替换 `ContentAreaPlaceholder`（已删除），接入 useAppEnvID/clusterId。
- `PodContentHeader`（收起/展开/刷新 + 详细·精简 Segmented，默认全展开+详细）、`PodFilterBar`（状态多选 + 屏蔽 Select + 搜索）+ `QuickFilters` 芯片组（全部/正常/异常/已屏蔽 + 全局计数）。双向联动：点芯片→设 status+blocked+quick；手改 status/blocked→quick=null；全部→清 status。

### Phase 4 — Pod 分组表格

- `GroupHeader`（三角展开、组名、kind Tag、版本 Popover(clusterName/currentVersion)、运行中/异常/已屏蔽/共N Badge、⋮ 菜单 = 后端 operations + 末尾"工作负载 YAML"占位，未选集群置灰）。
- `podCells.tsx` + `podColumns.tsx` + `PodGroupTable`：antd Table，每组独立分页 10/20/50、独立排序（restarts/creationTimestamp/status，服务端 sort 参数）、详细/精简双模式、状态徽章配色、GPU 列按组显隐、Pod名/IP 复制（Typography copyable）、操作列 ≤5 外露+更多、快捷批量选择走 antd rowSelection.selections（全选本页/反选/取消；全选所有·反选所有 按需求"暂时去掉"未做）。空/加载/错误(+重试) 态。

### Phase 5 — 跨组多选 + H 区显隐

- Decision：H 显隐用状态提升 + props（页面 `selectedCount` 驱动既有 `AnimatePresence`），非 createPortal 子元素探测（等价、更少样板）。`index.tsx` 移除 `mockVisible`/占位；`BatchActionBar` 展示"已选择 N 个实例"，跨组累计。

### 验证

- `yarn lint-type` 通过；`yarn build` 通过；`yarn test`（PodContentArea）**21/21 通过**（podStatus 4 + quickFilter 7 + duration 5 + filterParams 2 + selection 3）。
- `yarn lint`：新增文件无告警；仓库剩余 9 项 max-lines/any/unused 均为既有 lint 债（含 `verticalScale/rows.ts` 145 行），非本次引入。
- 探索性 1920×1080：内容区 flex 布局自适应主内容区宽度（antd Table `scroll.x` 横向滚动兜底）。

### 需求与关闭

- `pod-list-content-area.md` 状态 → 已实现（2026-07-25）。
- 数据缺口：服务暴露列占位（接口无字段）；CPU/内存使用率按 usages/limits 计算，缺 limits 时显示 '-'。
- 独立关闭审计（General subagent）PASS，无阻塞项；plan `planned → completed`。非阻塞注记：快捷批量选择走 antd 内置 selections（非 Figma 列头箭头）、操作列用通用占位图标（Non-Goal 内）、组头计数仅展开态加载（懒加载权衡）。

## 实现：工作负载页批量操作栏 + 批量 Pod 弹窗（子需求 3，plan 2026-07-25-workload-batch-operations）

执行 `docs/plans/2026-07-25-workload-batch-operations-plan.md`，owner `v_wangkaiyuan02`。draft review：General subagent PASS-WITH-NOTES（2 Blocking 已闭环：批量重启部分失败后置、selections 下拉与对象 map 同步）→ planned。

### Phase 1 — 选中态携带 Pod 对象 + 聚合逻辑

- `PodContentArea/selection.ts` 由 key-only 升级为 `SelectedPods = Record<groupId, Record<podKey, Pod>>`；`reconcileGroup`（onChange keys+rows，preserveSelectedRowKeys 兼容内置 selections 下拉，跨页保留对象）/`groupKeys`/`totalSelected`/`selectedList`。
- `BatchActionBar/batchActions.ts`：5 固定按钮常量 + `aggregateAction`（缺能力兜底、disabled 原因去重）。
- `PodGroupTable`/`PodContentArea` 改上报 Pod 对象；选中态提升至 `Workloads/index.tsx`。单测 selection 4 + batchActions 5。

### Phase 2 — BatchActionBar 业务内容

- 替换占位：计数 + 5 固定按钮（图标+文字、禁用置灰 + Tooltip 原因、强制删除红字 + 左分隔线）+ 关闭按钮（清空选中）。可用"重启/删除重建/强制删除"点击经 `onAction` 路由；屏蔽/解除屏蔽占位（点击不触发）。保留 AnimatePresence。

### Phase 3 — 集群参数数据链 + 批量重启弹窗

- `operations/batch/`：`useClusterParams`（Pod.workloadName→WorkloadGroup.workloads[].name→groupId→getRuntimeWorkloads→按 clusterId 取参数）、只读 `PodPreviewTable`、`ClusterParamsTable`（可编辑/只读两态）、`podTargets`（targets/集群去重/restart clusters 映射）。
- `operations/batchRestart/BatchRestartPodModal`（800、温馨提示、超时 5~3600、待重启预览、集群参数可编辑最大不可用 1~100%、提交 `restartPod`）。单测 podTargets 3。

### Phase 4 — 批量删除重建 + 强制删除弹窗

- `operations/batchDelete/DeleteModalBase`（Alert warning、只读预览+只读参数、提交 `deletePod({force})`）+ 两个薄封装 `BatchPodDeleteRebuildModal`(force:false)/`BatchPodForceDeleteModal`(force:true)，文案/标题/接口差异。

### 验证

- `yarn lint-type` 通过；`yarn build` 通过；`yarn test`（Workloads）**54/54 通过**（新增 selection 4 + batchActions 5 + podTargets 3）。
- `yarn lint`：新增文件无告警；仓库剩余 9 项为既有 lint 债。

### 需求与关闭

- `batch-action-bar.md` / `batch-restart-pod-modal.md` / `batch-pod-delete-rebuild-dialog.md` / `batch-pod-force-delete-dialog.md` 状态 → 已实现（2026-07-25）。
- 屏蔽/解除屏蔽按钮占位（后端能力/弹窗待补充）；批量重启"部分集群失败"行级详情后置（异步执行状态，已同步需求文档）。
- 独立关闭审计（General subagent）PASS，无阻塞项；plan `planned → completed`。非阻塞注记：batch-action-bar 需求残留 createPortal 描述已改为状态提升说明；重启弹窗 800 宽自适应高；"查看执行详情"为文字占位。

## 实现：Pod 详情抽屉（plan 2026-07-25-pod-detail-drawer）

执行 `docs/plans/2026-07-25-pod-detail-drawer-plan.md`，owner `v_wangkaiyuan02`。draft review：General subagent 初版 FAIL（真源冲突：端口/挂载/环境变量子字段实体已定义），修正需求「数据缺口汇总」+ 新增 Decision 项后收敛 planned。全部代码于 `src/pages/Workloads/PodContentArea/PodDetailDrawer/`。

### Phase 1 — 纯逻辑

- `containerOrder.ts`（合并 containers+initContainers 按 MAIN→NORMAL→SIDECAR→INIT 排序 + 类型 Badge）、`resourceUsage.ts`（CPU/内存格式化 + 使用率 + 高负载 + GPU 提取）、`podEventView.ts`（级别 tone + 相对时间 + 关键字过滤）。单测 9 项。

### Phase 2 — 抽屉骨架 + 打开入口

- 操作列前置固定"详情"图标 → `onOpenDetail` 贯通 podColumns→PodGroupTable→PodContentArea；`PodDetailDrawer`（antd Drawer 980px，getPodDetail，loading/error+重试）+ 标题栏（名称+状态 Tag+新窗口）+ 归属信息 + `BasicInfoCard`（Pod IP/节点 IP/版本占位/重启着色/存活/暴露占位，收起/展开）。

### Phase 3 — 容器区域 + 详细信息

- `ContainerArea`（容器 Tab 排序 + 类型 Badge + 圆点色；子 Tab 详细/日志/终端/事件）；`ContainerDetail` + `detailTables`（基本信息 + CPU/内存进度条使用率 + GPU 显隐；端口 protocol/name/port、挂载 type/mountPath/来源/readOnly、环境变量 name/value **真实渲染**，env 来源类型占位；标题灰色计数 N）。

### Phase 4 — 日志 / 终端 / 事件

- `ContainerLogs`（输出模式 segmented + 暂停↔开始 / 全屏↔退出 toggle，内容占位）、`ContainerTerminal`（连接↔断开 toggle + 清屏未连接禁用，内容占位）、`ContainerEvents`（真实 getPodEvents 按 container 过滤 + 级别筛选 + 客户端搜索 + 游标 token 栈上一页/下一页）。

### 验证

- `yarn lint-type` 通过；`yarn build` 通过；`yarn test`（PodDetailDrawer）**9/9 通过**；`yarn lint` 新增文件无告警（仓库剩余 9 项既有债）。

### 需求与关闭

- `pod-detail-drawer.md` 状态 → 已实现；真源修正：端口/挂载/环境变量子字段实体已定义→真实渲染，仅 env 来源类型/版本/暴露/上一次终止/GPU 型号占位。
- 日志/终端内容渲染（xterm/WebSocket）后置；独立关闭审计见 plan Closure Gates。
- 独立关闭审计（General subagent）：B1（上一次终止占位描述与代码不一致）已按审计建议以真源方式修正——接口无 lastState.terminated 字段，按 BR6 无记录即隐藏、本期不渲染，需求/plan 对齐；其余 PASS，plan `planned → completed`。

## 实现：能力注册表落地与消费方收敛（plan 2026-07-25-capability-registry-consolidation）

执行 `docs/plans/2026-07-25-capability-registry-consolidation-plan.md`，owner `v_wangkaiyuan02`。draft review：General subagent 初版 FAIL（批量成员按 supportsBatch 派生会误删 block/unblock 占位，B1）→ 改为按 `targetKind==='Pod'` 派生 + supportsBatch 仅表"是否已实现" + 采纳 3 建议后 planned。纯内部重构，行为零变化。

### 改动

- `domain/workload/capability.ts`：`CapabilityDef` 增 `dialog?: DialogKey`；登记全部能力（Vertical/Horizontal/Restart=Workload+dialog；PodRestart/PodDelete/PodDeleteForce=Pod+supportsBatch+dialog；PodBlock/PodUnblock/ApplicationUninstall=占位无 dialog）；新增派生纯函数 `listModalCapabilities()`（targetKind None/Workload 且有 dialog）/ `listBatchCapabilities()`（targetKind==='Pod'）；顶部注释写入"批量能力前端写死"的理由（当前批量固定 5 个）。`index.ts` 同步导出。
- `WorkloadsHeader/OperationModals.tsx`：删除硬编码 `MODAL_CAPABILITIES` 数组与 capability→弹窗 `if` 链，改为 `MODAL_CAPABILITIES = new Set(listModalCapabilities())` + `dialog` key→组件表派生路由。
- `BatchActionBar/batchActions.ts`：`BATCH_ACTIONS` 由 UI 展示表 `BATCH_ACTION_PRESENTATION` ∩ `listBatchCapabilities()` 派生（成员来源注册表，顺序/label/danger/placeholder 留展示表）；`aggregateAction` 不变。

### 验证

- `yarn lint-type` 通过；`yarn build` 通过；`yarn test`（domain/workload + BatchActionBar）**25/25 通过**（capability 扩至 7：注册表条目 + 两派生选择器 + 占位；batchActions 5 项集合/顺序/danger/placeholder 等价）。
- `yarn lint`：改动文件无告警；仓库剩余 9 项为既有 lint 债。
- 行为零变化：标题栏 3 弹窗路由、批量栏 5 按钮（含 block/unblock 占位）集合/顺序均与改造前一致。

### 文档与关闭

- `docs/architecture/workload-domain-model.md` L3 Capability Registry 由"目标设计"改为"当前基线"（标注已落地消费方 + 尚未收敛项：OperationCommand 统一/PodOperation 合并/行内操作），`CapabilityDef` 草图同步（加 dialog、buildTargets/buildParams 标后续）。
- OperationCommand 提交路径统一为 Non-Goal，后续切片评估。独立关闭审计（General subagent）PASS，无阻塞项；plan `planned → completed`。

## 工具：ai-context / ai-capabilities 生成物稳定化（方案 A）+ public/README

- `scripts/generate-ai-context.ts`：移除两个产物的 volatile `generatedAt` 时间戳；`src/types/aiExecutor.ts` 的 `AIContext` 同步去掉 `generatedAt` 字段（无消费方）。此前每次 `yarn build` 仅时间戳变化即产生 diff，污染 git；去掉后 `public/ai-context.json` / `ai-capabilities.json` 仅在路由/能力真正变化时才变更。
- 决策：保留这两个生成物在仓库（不 gitignore）——`yarn start`(dev) 不跑 gen，运行时 `fetch('/ai-context.json')`，忽略会导致新克隆 dev 下 404。
- 新增 `public/README.md`：说明两文件作用（页面路由地图 / AI 可调用能力清单）、生成方式（`yarn gen:ai-context`，build 前置）、以及"提交入仓 + 去时间戳"的设计理由。
- 验证：`yarn gen:ai-context` / `yarn lint-type` / `yarn build` 通过；两 json 的 diff 仅为移除 `generatedAt`，内容不变。
