# 2026-07-22

## 新增

- 创建 `docs/requirements/vertical-scale-dialog.md` — 纵向扩缩弹窗需求文档
  - 基于 Figma 设计稿「Frame 1912057556」整理
  - 包含弹窗结构、配置表格、交互需求、与源设计文档差异对比、待澄清场景
  - 映射卡片：暂无（设计稿驱动）

- 创建 `docs/requirements/batch-restart-pod-modal.md` — 批量重启 Pod 弹窗需求文档
  - 基于 Figma 设计稿「Frame 1912057557」整理
  - 与现有「应用重启弹窗」区分：触发入口为 Pod 列表 BatchActionBar，展示待重启 Pod 列表预览，集群由所选 Pod 自动聚合
  - 包含弹窗结构、Pod 预览表格、集群参数配置、交互逻辑、边界状态
  - 父需求：`docs/requirements/workloads-page.md`（子需求 3 — H 区域业务逻辑）
  - 映射卡片：暂无（设计稿驱动）

- 创建 `docs/requirements/horizontal-scale-dialog.md` — 横向扩缩弹窗需求文档
  - 基于 Figma 设计稿「Frame 1912057553」整理
  - 分析了弹窗结构：头部（标题、环境、功能介绍）、配置表格（复选框/集群/当前副本数/期望副本数/最大不可用/可用度锁）、底部操作栏（提示文字 + 取消/确定按钮）
  - 将设计稿中的占位性文字（如"环境名称"、"15"、"4"、"15%"等）转换为需求性描述
  - 补充了与纵向扩缩弹窗的差异对比
  - 列出了未覆盖需进一步澄清的场景

## 验证状态

- 未验证（文档类任务，无需构建）

## 接口文档同步 - 运行时工作负载 API

- 从 ku 知识库重新抓取最新接口文档，对比发现 8 处差异，忽略 clusterID 大小写，调整其余 7 项：
  1. **RuntimeSummary 响应结构**：`podStatuses: PodStatusSummary[]` → `podStatistics: { totalCount, blockedCount, statuses[] }`（`src/interface/entities/runtimeSummary.ts`）
  2. **RuntimeSummary query 参数**：`clusterID` → `clusterId`，新增 `groupId`（`src/api/runtimeResource.ts`）
  3. **PodList 响应新增 `summary` 字段**（`src/interface/entities/pod.ts`）
  4. **Operation mock 数据修正**：`scale-h`/`scale-v` 的 capability 从 `ApplicationRestart` 改为 `HorizontalScale`/`VerticalScale`（`src/api/runtimeOperation.ts`）
  5. **Pod 状态展示规则**：匹配中文名展示，未匹配展示原始 status，hover 原始值（更新 `docs/input/source-api-runtime-workloads.md`）
  6. **快捷筛选公式**：全部/正常/异常/已屏蔽 的计算规则（更新 `docs/input/source-api-runtime-workloads.md` 和 `docs/requirements/pod-list-content-area.md`）
  7. **容器排序规则**：MAIN → NORMAL → SIDECAR → INIT（更新 `docs/input/source-api-runtime-workloads.md`）

## 第二次接口文档同步 - 运行时工作负载 API

- 从 ku 知识库再次抓取最新接口文档，对比发现 7 处差异：

| # | 变更                                                                               | 影响文件                                                                      |
| - | ---------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| 1 | OperationCapability: `ApplicationRestart` → `Restart`                              | `src/interface/entities/runtimeOperation.ts`                                  |
| 2 | OperationTargetKind: 新增 `Workload`                                               | `src/interface/entities/runtimeOperation.ts`                                  |
| 3 | RuntimeOperation / PodOperation: 新增 `supportsBatch` 字段                         | `src/interface/entities/runtimeOperation.ts`, `src/interface/entities/pod.ts` |
| 4 | OperationTarget: 新增 `container`, `params` 字段                                   | `src/interface/entities/runtimeOperation.ts`                                  |
| 5 | Container log query params: 移除 `timestamps`，新增 `source`, `filePath`, `follow` | `src/api/runtimeResource.ts`                                                  |
| 6 | 新增接口 `GET /runtime/workloads` (Restart 操作获取 workload 列表)                 | 已在接口文档中记录，API 模块待后续添加                                        |
| 7 | `docs/input/source-api-runtime-workloads.md` 全面替换为最新版                      | 文档更新                                                                      |

- 用户自行清理了 `runtimeOperation.ts` 中的 mock 数据
- `yarn lint-type` 通过

## 补充需求文档实现规则

将接口文档中新增的 3 条实现规则写入对应需求文档 `docs/requirements/pod-list-content-area.md`：

1. **Pod 状态展示规则**：中文名匹配展示 + 未匹配展示原始 status + hover 原始值 + 状态徽章颜色映射 + 状态下拉数据来源
2. **快捷筛选公式**：计数数据来源（`summary.totalCount`/`blockedCount`）及每个按钮的筛选动作
3. **容器排序规则**（待 Pod 详情页实现时遵循）：MAIN → NORMAL → SIDECAR → INIT
